// Code generated by internal/cmd/cligen; DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"

	flashduty "github.com/flashcatcloud/go-flashduty"
)

func genStatusPagesReadPageListCmd() *cobra.Command {
	var dataJSON string
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List status pages",
		Long: `List status pages.

List all status pages owned by the account, including their components and sections.

API: GET /status-page/list (status-page-read-page-list)

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>) — Status pages owned by the account.
    - components (array<object>) — Components tracked on the status page.
      - available_since_seconds (integer) — Timestamp when the component was first available, in unix seconds.
      - component_id (string) — Component ID.
      - description (string) — Component description.
      - hide_all (boolean) — When true, the component is hidden entirely from summary endpoints.
      - hide_uptime (boolean) — When true, uptime data is hidden from summary responses.
      - name (string) (required) — Component display name.
      - order_id (integer) — Display order within its section.
      - section_id (string) — Parent section ID.
    - contact_info (string) — Get-in-touch contact, a mailto or website URL.
    - custom_domain (string) — Custom domain pointing to the status page.
    - custom_links (array<object>) — Custom navigation links shown on the status page.
    - dark_logo (string) — Dark-mode logo image of the status page.
    - date_view (string) — How the timeline is displayed. [calendar, list]
    - display_uptime_mode (string) — How uptime is displayed. [chart_and_percentage, chart, none]
    - favicon (string) — Favicon of the status page.
    - logo (string) — Logo image of the status page.
    - logo_url (string) — URL opened when the logo is clicked.
    - name (string) — Display name of the status page.
    - page_footer (string) — Footer content of the status page.
    - page_header (string) — Header content of the status page.
    - page_id (integer) — Status page ID.
    - sections (array<object>) — Sections grouping the components.
      - description (string) — Section description.
      - hide_all (boolean) — Whether the section and its components are hidden from summary endpoints.
      - hide_uptime (boolean) — Whether uptime data is hidden from summary responses.
      - name (string) — Section name.
      - order_id (integer) — Display order of the section.
      - section_id (string) — Section ID.
    - subscription (object)
      - email (boolean) — Whether email subscription is enabled.
      - im (boolean) — Whether IM subscription is enabled.
    - template_preference (string) — Preferred change-event template type.
    - type (string) — Visibility type of the status page. [public, internal]
    - url_name (string) — URL-safe slug, unique per account.
`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
				})
				if err != nil {
					return err
				}
				_ = body
				out, _, err := ctx.Client.StatusPages.ReadPageList(cmdContext(ctx.Cmd))
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeActiveListCmd() *cobra.Command {
	var dataJSON string
	var fPageID int64
	var fType string
	cmd := &cobra.Command{
		Use:   "change-active-list",
		Short: "List active status page events",
		Long: `List active status page events.

List in-progress (non-terminal) events of a given type for a status page.

API: GET /status-page/change/active/list (statusPageChangeActiveList)

Request fields:
  --page-id int (required) — Status page ID.
  --type string (required) — Event type filter. Required. Returns only in-progress (non-terminal) events — 'investigating'/'identified'/'monitoring' for 'incident', 'scheduled'/'ongoing' for 'maintenance'. [incident, maintenance]

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>) (required)
    - affected_components (array<object>) — Components currently affected by this event, with their resulting status.
      - available_since_seconds (integer) — Timestamp when the component was first available, in unix seconds.
      - component_id (string) — Component ID.
      - description (string) — Component description.
      - hide_all (boolean) — When true, the component is hidden entirely from summary endpoints.
      - hide_uptime (boolean) — When true, uptime data is hidden from summary responses.
      - name (string) (required) — Component display name.
      - order_id (integer) — Display order within its section.
      - section_id (string) — Parent section ID.
      - status (string) (required) — Current component status resulting from the event. [operational, degraded, partial_outage, full_outage, under_maintenance]
    - auto_update_by_schedule (boolean) — Maintenance only: whether the status advances automatically based on the scheduled window.
    - change_id (integer) (required) — Event ID.
    - close_at_seconds (integer) — Scheduled close time in unix seconds. Set for retrospective and maintenance events.
    - description (string) — Event description (Markdown).
    - is_retrospective (boolean) — Whether this event is a retrospective (historical) one.
    - linked_change_ids (array<string>) — Linked event IDs (related incidents, deployments, etc.).
    - notify_subscribers (boolean) — Whether subscribers were notified about this event.
    - page_id (integer) — Parent status page ID.
    - responder_ids (array<integer>) — Member IDs responsible for this event.
    - start_at_seconds (integer) — Event start time in unix seconds.
    - status (string) — Current event status. Incident statuses: 'investigating'/'identified'/'monitoring'/'resolved'. Maintenance statuses: 'scheduled'/'ongoing'/'completed'. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
    - title (string) (required) — Event title.
    - type (string) (required) — Event type. [incident, maintenance]
    - updates (array<object>) — Timeline updates attached to this event, ordered by time.
      - at_seconds (integer) (required) — Update timestamp in unix seconds.
      - component_changes (array<object>) — Component status transitions applied by this update.
        - component_id (string) (required) — Component ID.
        - component_name (string) — Component display name. Populated by the backend on read; ignored on write.
        - status (string) (required) — New component status. Incidents support 'operational'/'degraded'/'partial_outage'/'full_outage'; maintenances support 'operational'/'under_maintenance'. [operational, degraded, partial_outage, full_outage, under_maintenance]
      - description (string) — Update description (Markdown).
      - status (string) — Event status after this update. Omitted when the update does not change the overall status. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
      - update_id (string) (required) — Update ID.
`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("type") {
						body["type"] = fType
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.StatusPagesChangeActiveListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.ChangeActiveList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&fType, "type", "", "Event type filter. Required. Returns only in-progress (non-terminal) events — 'investigating'/'identified'/'monitoring' for 'incident', 'scheduled'/'ongoing' for 'maintenance'. (required) [incident, maintenance]")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeCreateCmd() *cobra.Command {
	var dataJSON string
	var fAutoUpdateBySchedule bool
	var fCloseAtSeconds string
	var fDescription string
	var fIsRetrospective bool
	var fLinkedChanges []string
	var fNotifySubscribers bool
	var fPageID int64
	var fResponders []int
	var fStartAtSeconds string
	var fStatus string
	var fTitle string
	var fType string
	cmd := &cobra.Command{
		Use:   "change-create",
		Short: "Create status page event",
		Long: `Create status page event.

Create a new incident or maintenance event on a status page.

API: POST /status-page/change/create (statusPageChangeCreate)

Request fields:
  --auto-update-by-schedule bool — Maintenance only: automatically advance the status based on the scheduled window.
  --close-at-seconds string — Scheduled close time for retrospective events. Must be greater than 'start_at_seconds'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --description string — Event description (Markdown). Required by the validator.
  --is-retrospective bool — Mark this event as a retrospective (historical) one.
  --linked-changes []string — Linked change IDs (related incidents, deployments, etc.).
  --notify-subscribers bool — Notify subscribers about this event and all its updates.
  --page-id int (required) — Status page ID.
  --responders []int — Member IDs responsible for this event.
  --start-at-seconds string — Event start time in unix seconds. Defaults to now when omitted. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --status string (required) — Initial event status. 'investigating'/'identified'/'monitoring'/'resolved' apply to incidents; 'scheduled'/'ongoing'/'completed' apply to maintenances. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
  --title string (required) — Event title, up to 255 characters. (≤255 chars)
  --type string (required) — Event type. [incident, maintenance]
  updates (array<object>, via --data) (required) — Timeline updates. Immediate events normally pass one update; retrospective events must pass all historical updates.
    - at_seconds (integer) — Update timestamp in unix seconds.
    - component_changes (array<object>) — Component status transitions applied by this update.
      - component_id (string) (required) — Component ID.
      - status (string) (required) — New component status. 'operational'/'degraded'/'partial_outage'/'full_outage' apply to incidents; 'operational'/'under_maintenance' apply to maintenances. [operational, degraded, partial_outage, full_outage, under_maintenance]
    - description (string) — Update description (Markdown).
    - status (string) — Change status after this update. Omit if the overall status does not change. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
    - update_id (string) — Update ID. Server-assigned on create; supply when replaying historical updates.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - change_id (integer) (required) — Newly created event ID.
  - change_name (string) (required) — Event title (echoed from the request).
`,
		Example: `  flashduty status-page change-create --data '{"description":"We are investigating degraded performance affecting the web console.","notify_subscribers":true,"page_id":5750613685214,"start_at_seconds":1712000000,"status":"investigating","title":"Web Console Degraded Performance","type":"incident","updates":[{"component_changes":[{"component_id":"01KC3GAZ6ZJE40H55GM31RPWZE","status":"degraded"}],"description":"We are currently investigating an issue affecting some users.","status":"investigating"}]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vCloseAtSeconds, okCloseAtSeconds, err := genParseTimeFlag(cmd, "close-at-seconds", fCloseAtSeconds)
				if err != nil {
					return err
				}
				vStartAtSeconds, okStartAtSeconds, err := genParseTimeFlag(cmd, "start-at-seconds", fStartAtSeconds)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("auto-update-by-schedule") {
						body["auto_update_by_schedule"] = fAutoUpdateBySchedule
					}
					if okCloseAtSeconds {
						body["close_at_seconds"] = vCloseAtSeconds
					}
					if cmd.Flags().Changed("description") {
						body["description"] = fDescription
					}
					if cmd.Flags().Changed("is-retrospective") {
						body["is_retrospective"] = fIsRetrospective
					}
					if cmd.Flags().Changed("linked-changes") {
						body["linked_changes"] = fLinkedChanges
					}
					if cmd.Flags().Changed("notify-subscribers") {
						body["notify_subscribers"] = fNotifySubscribers
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("responders") {
						body["responders"] = fResponders
					}
					if okStartAtSeconds {
						body["start_at_seconds"] = vStartAtSeconds
					}
					if cmd.Flags().Changed("status") {
						body["status"] = fStatus
					}
					if cmd.Flags().Changed("title") {
						body["title"] = fTitle
					}
					if cmd.Flags().Changed("type") {
						body["type"] = fType
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.CreateStatusPageChangeRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.ChangeCreate(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().BoolVar(&fAutoUpdateBySchedule, "auto-update-by-schedule", false, "Maintenance only: automatically advance the status based on the scheduled window.")
	cmd.Flags().StringVar(&fCloseAtSeconds, "close-at-seconds", "", "Scheduled close time for retrospective events. Must be greater than 'start_at_seconds'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringVar(&fDescription, "description", "", "Event description (Markdown). Required by the validator.")
	cmd.Flags().BoolVar(&fIsRetrospective, "is-retrospective", false, "Mark this event as a retrospective (historical) one.")
	cmd.Flags().StringSliceVar(&fLinkedChanges, "linked-changes", nil, "Linked change IDs (related incidents, deployments, etc.).")
	cmd.Flags().BoolVar(&fNotifySubscribers, "notify-subscribers", false, "Notify subscribers about this event and all its updates.")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().IntSliceVar(&fResponders, "responders", nil, "Member IDs responsible for this event.")
	cmd.Flags().StringVar(&fStartAtSeconds, "start-at-seconds", "", "Event start time in unix seconds. Defaults to now when omitted. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringVar(&fStatus, "status", "", "Initial event status. 'investigating'/'identified'/'monitoring'/'resolved' apply to incidents; 'scheduled'/'ongoing'/'completed' apply to maintenances. (required) [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]")
	cmd.Flags().StringVar(&fTitle, "title", "", "Event title, up to 255 characters. (required) (≤255 chars)")
	cmd.Flags().StringVar(&fType, "type", "", "Event type. (required) [incident, maintenance]")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeDeleteCmd() *cobra.Command {
	var dataJSON string
	var fChangeID int64
	var fPageID int64
	cmd := &cobra.Command{
		Use:   "change-delete",
		Short: "Delete status page event",
		Long: `Delete status page event.

Delete a status page event.

API: POST /status-page/change/delete (statusPageChangeDelete)

Request fields:
  --change-id int (required) — Target event ID.
  --page-id int (required) — Status page ID.
`,
		Example: `  flashduty status-page change-delete --data '{"change_id":5821693893131,"page_id":5750613685214}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("change-id") {
						body["change_id"] = fChangeID
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.DeleteStatusPageChangeRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.StatusPages.ChangeDelete(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /status-page/change/delete")
				return nil
			})
		},
	}
	cmd.Flags().Int64Var(&fChangeID, "change-id", 0, "Target event ID. (required)")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeInfoCmd() *cobra.Command {
	var dataJSON string
	var fPageID int64
	var fChangeID int64
	cmd := &cobra.Command{
		Use:   "change-info",
		Short: "Get status page event detail",
		Long: `Get status page event detail.

Retrieve details of a specific status page event (incident or maintenance).

API: GET /status-page/change/info (statusPageChangeInfo)

Request fields:
  --page-id int (required) — Status page ID.
  --change-id int (required) — Event (change) ID.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - affected_components (array<object>) — Components currently affected by this event, with their resulting status.
    - available_since_seconds (integer) — Timestamp when the component was first available, in unix seconds.
    - component_id (string) — Component ID.
    - description (string) — Component description.
    - hide_all (boolean) — When true, the component is hidden entirely from summary endpoints.
    - hide_uptime (boolean) — When true, uptime data is hidden from summary responses.
    - name (string) (required) — Component display name.
    - order_id (integer) — Display order within its section.
    - section_id (string) — Parent section ID.
    - status (string) (required) — Current component status resulting from the event. [operational, degraded, partial_outage, full_outage, under_maintenance]
  - auto_update_by_schedule (boolean) — Maintenance only: whether the status advances automatically based on the scheduled window.
  - change_id (integer) (required) — Event ID.
  - close_at_seconds (integer) — Scheduled close time in unix seconds. Set for retrospective and maintenance events.
  - description (string) — Event description (Markdown).
  - is_retrospective (boolean) — Whether this event is a retrospective (historical) one.
  - linked_change_ids (array<string>) — Linked event IDs (related incidents, deployments, etc.).
  - notify_subscribers (boolean) — Whether subscribers were notified about this event.
  - page_id (integer) — Parent status page ID.
  - responder_ids (array<integer>) — Member IDs responsible for this event.
  - start_at_seconds (integer) — Event start time in unix seconds.
  - status (string) — Current event status. Incident statuses: 'investigating'/'identified'/'monitoring'/'resolved'. Maintenance statuses: 'scheduled'/'ongoing'/'completed'. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
  - title (string) (required) — Event title.
  - type (string) (required) — Event type. [incident, maintenance]
  - updates (array<object>) — Timeline updates attached to this event, ordered by time.
    - at_seconds (integer) (required) — Update timestamp in unix seconds.
    - component_changes (array<object>) — Component status transitions applied by this update.
      - component_id (string) (required) — Component ID.
      - component_name (string) — Component display name. Populated by the backend on read; ignored on write.
      - status (string) (required) — New component status. Incidents support 'operational'/'degraded'/'partial_outage'/'full_outage'; maintenances support 'operational'/'under_maintenance'. [operational, degraded, partial_outage, full_outage, under_maintenance]
    - description (string) — Update description (Markdown).
    - status (string) — Event status after this update. Omitted when the update does not change the overall status. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
    - update_id (string) (required) — Update ID.
`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("change-id") {
						body["change_id"] = fChangeID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.StatusPagesChangeInfoRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.ChangeInfo(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().Int64Var(&fChangeID, "change-id", 0, "Event (change) ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeListCmd() *cobra.Command {
	var dataJSON string
	var fPageID int64
	var fStartAtSeconds string
	var fEndAtSeconds string
	var fType string
	var fStatus string
	cmd := &cobra.Command{
		Use:   "change-list",
		Short: "List status page events",
		Long: `List status page events.

List events (incidents and maintenances) for a status page.

API: GET /status-page/change/list (statusPageChangeList)

Request fields:
  --page-id int (required) — Status page ID.
  --start-at-seconds string — Filter events started at or after this unix timestamp (seconds). Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --end-at-seconds string — Filter events started at or before this unix timestamp (seconds). Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --type string (required) — Event type filter. Required. [incident, maintenance]
  --status string (required) — Event status filter. Required. Must be a status valid for the given 'type' (e.g. 'investigating'/'identified'/'monitoring'/'resolved' for incidents; 'scheduled'/'ongoing'/'completed' for maintenances). [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>) (required)
    - affected_components (array<object>) — Components currently affected by this event, with their resulting status.
      - available_since_seconds (integer) — Timestamp when the component was first available, in unix seconds.
      - component_id (string) — Component ID.
      - description (string) — Component description.
      - hide_all (boolean) — When true, the component is hidden entirely from summary endpoints.
      - hide_uptime (boolean) — When true, uptime data is hidden from summary responses.
      - name (string) (required) — Component display name.
      - order_id (integer) — Display order within its section.
      - section_id (string) — Parent section ID.
      - status (string) (required) — Current component status resulting from the event. [operational, degraded, partial_outage, full_outage, under_maintenance]
    - auto_update_by_schedule (boolean) — Maintenance only: whether the status advances automatically based on the scheduled window.
    - change_id (integer) (required) — Event ID.
    - close_at_seconds (integer) — Scheduled close time in unix seconds. Set for retrospective and maintenance events.
    - description (string) — Event description (Markdown).
    - is_retrospective (boolean) — Whether this event is a retrospective (historical) one.
    - linked_change_ids (array<string>) — Linked event IDs (related incidents, deployments, etc.).
    - notify_subscribers (boolean) — Whether subscribers were notified about this event.
    - page_id (integer) — Parent status page ID.
    - responder_ids (array<integer>) — Member IDs responsible for this event.
    - start_at_seconds (integer) — Event start time in unix seconds.
    - status (string) — Current event status. Incident statuses: 'investigating'/'identified'/'monitoring'/'resolved'. Maintenance statuses: 'scheduled'/'ongoing'/'completed'. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
    - title (string) (required) — Event title.
    - type (string) (required) — Event type. [incident, maintenance]
    - updates (array<object>) — Timeline updates attached to this event, ordered by time.
      - at_seconds (integer) (required) — Update timestamp in unix seconds.
      - component_changes (array<object>) — Component status transitions applied by this update.
        - component_id (string) (required) — Component ID.
        - component_name (string) — Component display name. Populated by the backend on read; ignored on write.
        - status (string) (required) — New component status. Incidents support 'operational'/'degraded'/'partial_outage'/'full_outage'; maintenances support 'operational'/'under_maintenance'. [operational, degraded, partial_outage, full_outage, under_maintenance]
      - description (string) — Update description (Markdown).
      - status (string) — Event status after this update. Omitted when the update does not change the overall status. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
      - update_id (string) (required) — Update ID.
`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vStartAtSeconds, okStartAtSeconds, err := genParseTimeFlag(cmd, "start-at-seconds", fStartAtSeconds)
				if err != nil {
					return err
				}
				vEndAtSeconds, okEndAtSeconds, err := genParseTimeFlag(cmd, "end-at-seconds", fEndAtSeconds)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if okStartAtSeconds {
						body["start_at_seconds"] = vStartAtSeconds
					}
					if okEndAtSeconds {
						body["end_at_seconds"] = vEndAtSeconds
					}
					if cmd.Flags().Changed("type") {
						body["type"] = fType
					}
					if cmd.Flags().Changed("status") {
						body["status"] = fStatus
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.StatusPagesChangeListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.ChangeList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&fStartAtSeconds, "start-at-seconds", "", "Filter events started at or after this unix timestamp (seconds). Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringVar(&fEndAtSeconds, "end-at-seconds", "", "Filter events started at or before this unix timestamp (seconds). Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringVar(&fType, "type", "", "Event type filter. Required. (required) [incident, maintenance]")
	cmd.Flags().StringVar(&fStatus, "status", "", "Event status filter. Required. Must be a status valid for the given 'type' (e.g. 'investigating'/'identified'/'monitoring'/'resolved' for incidents; 'scheduled'/'ongoing'/'completed' for maintenances). (required) [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeTimelineCreateCmd() *cobra.Command {
	var dataJSON string
	var fAtSeconds string
	var fChangeID int64
	var fDescription string
	var fPageID int64
	var fStatus string
	cmd := &cobra.Command{
		Use:   "change-timeline-create",
		Short: "Create event timeline entry",
		Long: `Create event timeline entry.

Add a timeline update to a status page event.

API: POST /status-page/change/timeline/create (statusPageChangeTimelineCreate)

Request fields:
  --at-seconds string — Update timestamp in unix seconds. Defaults to now when omitted. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --change-id int (required) — Target event ID.
  --description string — Update description (Markdown). Required.
  --page-id int (required) — Status page ID.
  --status string (required) — New event status. Must match the event type. When the status transitions to 'resolved' or 'completed', all referenced components must become 'operational'. [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]
  component_changes (array<object>, via --data) — Component status transitions applied by this update. Component IDs must be unique.
    - component_id (string) (required) — Component ID.
    - status (string) (required) — New component status. 'operational'/'degraded'/'partial_outage'/'full_outage' apply to incidents; 'operational'/'under_maintenance' apply to maintenances. [operational, degraded, partial_outage, full_outage, under_maintenance]

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - update_id (string) (required) — Newly created update ID.
`,
		Example: `  flashduty status-page change-timeline-create --data '{"at_seconds":1712003600,"change_id":5821693893131,"component_changes":[{"component_id":"01KC3GAZ6ZJE40H55GM31RPWZE","status":"partial_outage"}],"description":"We have identified the root cause and are working on a fix.","page_id":5750613685214,"status":"identified"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vAtSeconds, okAtSeconds, err := genParseTimeFlag(cmd, "at-seconds", fAtSeconds)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if okAtSeconds {
						body["at_seconds"] = vAtSeconds
					}
					if cmd.Flags().Changed("change-id") {
						body["change_id"] = fChangeID
					}
					if cmd.Flags().Changed("description") {
						body["description"] = fDescription
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("status") {
						body["status"] = fStatus
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.CreateStatusPageChangeTimelineRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.ChangeTimelineCreate(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAtSeconds, "at-seconds", "", "Update timestamp in unix seconds. Defaults to now when omitted. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().Int64Var(&fChangeID, "change-id", 0, "Target event ID. (required)")
	cmd.Flags().StringVar(&fDescription, "description", "", "Update description (Markdown). Required.")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&fStatus, "status", "", "New event status. Must match the event type. When the status transitions to 'resolved' or 'completed', all referenced components must become 'operational'. (required) [investigating, identified, monitoring, resolved, scheduled, ongoing, completed]")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeTimelineDeleteCmd() *cobra.Command {
	var dataJSON string
	var fChangeID int64
	var fPageID int64
	var fUpdateID string
	cmd := &cobra.Command{
		Use:   "change-timeline-delete",
		Short: "Delete event timeline entry",
		Long: `Delete event timeline entry.

Delete a timeline entry from a status page event.

API: POST /status-page/change/timeline/delete (statusPageChangeTimelineDelete)

Request fields:
  --change-id int (required) — Parent event ID.
  --page-id int (required) — Status page ID.
  --update-id string (required) — Timeline update ID to delete.
`,
		Example: `  flashduty status-page change-timeline-delete --data '{"change_id":5821693893131,"page_id":5750613685214,"update_id":"01KP0311872NVYFRRQ82FWXAP4"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("change-id") {
						body["change_id"] = fChangeID
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("update-id") {
						body["update_id"] = fUpdateID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.DeleteStatusPageChangeTimelineRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.StatusPages.ChangeTimelineDelete(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /status-page/change/timeline/delete")
				return nil
			})
		},
	}
	cmd.Flags().Int64Var(&fChangeID, "change-id", 0, "Parent event ID. (required)")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&fUpdateID, "update-id", "", "Timeline update ID to delete. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeTimelineUpdateCmd() *cobra.Command {
	var dataJSON string
	var fAtSeconds string
	var fChangeID int64
	var fDescription string
	var fPageID int64
	var fUpdateID string
	cmd := &cobra.Command{
		Use:   "change-timeline-update",
		Short: "Update event timeline entry",
		Long: `Update event timeline entry.

Update a timeline entry for a status page event.

API: POST /status-page/change/timeline/update (statusPageChangeTimelineUpdate)

Request fields:
  --at-seconds string — New update timestamp in unix seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --change-id int (required) — Parent event ID.
  --description string — New update description (Markdown).
  --page-id int (required) — Status page ID.
  --update-id string (required) — Target timeline update ID.
`,
		Example: `  flashduty status-page change-timeline-update --data '{"at_seconds":1712003600,"change_id":5821693893131,"description":"Corrected description: root cause identified in database layer.","page_id":5750613685214,"update_id":"01KP0311872NVYFRRQ82FWXAP4"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vAtSeconds, okAtSeconds, err := genParseTimeFlag(cmd, "at-seconds", fAtSeconds)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if okAtSeconds {
						body["at_seconds"] = vAtSeconds
					}
					if cmd.Flags().Changed("change-id") {
						body["change_id"] = fChangeID
					}
					if cmd.Flags().Changed("description") {
						body["description"] = fDescription
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("update-id") {
						body["update_id"] = fUpdateID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.UpdateStatusPageChangeTimelineRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.StatusPages.ChangeTimelineUpdate(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /status-page/change/timeline/update")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fAtSeconds, "at-seconds", "", "New update timestamp in unix seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().Int64Var(&fChangeID, "change-id", 0, "Parent event ID. (required)")
	cmd.Flags().StringVar(&fDescription, "description", "", "New update description (Markdown).")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&fUpdateID, "update-id", "", "Target timeline update ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesChangeUpdateCmd() *cobra.Command {
	var dataJSON string
	var fChangeID int64
	var fLinkedChanges []string
	var fPageID int64
	var fResponders []int
	var fTitle string
	cmd := &cobra.Command{
		Use:   "change-update",
		Short: "Update status page event",
		Long: `Update status page event.

Update an existing status page event.

API: POST /status-page/change/update (statusPageChangeUpdate)

Request fields:
  --change-id int (required) — Target event ID.
  --linked-changes []string — Linked event IDs. Pass the full replacement list.
  --page-id int (required) — Status page ID.
  --responders []int — Member IDs responsible for this event. Pass the full replacement list.
  --title string — New event title, up to 255 characters. Omit to keep the existing value. (≤255 chars)
`,
		Example: `  flashduty status-page change-update --data '{"change_id":5821693893131,"page_id":5750613685214,"title":"Web Console Degraded Performance (Updated)"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("change-id") {
						body["change_id"] = fChangeID
					}
					if cmd.Flags().Changed("linked-changes") {
						body["linked_changes"] = fLinkedChanges
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("responders") {
						body["responders"] = fResponders
					}
					if cmd.Flags().Changed("title") {
						body["title"] = fTitle
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.UpdateStatusPageChangeRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.StatusPages.ChangeUpdate(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /status-page/change/update")
				return nil
			})
		},
	}
	cmd.Flags().Int64Var(&fChangeID, "change-id", 0, "Target event ID. (required)")
	cmd.Flags().StringSliceVar(&fLinkedChanges, "linked-changes", nil, "Linked event IDs. Pass the full replacement list.")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().IntSliceVar(&fResponders, "responders", nil, "Member IDs responsible for this event. Pass the full replacement list.")
	cmd.Flags().StringVar(&fTitle, "title", "", "New event title, up to 255 characters. Omit to keep the existing value. (≤255 chars)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesMigrateEmailSubscribersCmd() *cobra.Command {
	var dataJSON string
	var fAPIKey string
	var fSourcePageID string
	var fTargetPageID int64
	cmd := &cobra.Command{
		Use:   "migrate-email-subscribers",
		Short: "Migrate email subscribers",
		Long: `Migrate email subscribers.

Start a migration job that imports email subscribers from an Atlassian Statuspage into an existing Flashduty status page.

API: POST /status-page/migrate-email-subscribers (statusPageMigrateEmailSubscribers)

Request fields:
  --api-key string (required) — Atlassian Statuspage API key with access to the source page.
  --source-page-id string (required) — Atlassian Statuspage source page ID.
  --target-page-id int (required) — Flashduty target status page ID that will receive the imported subscribers.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - job_id (string) (required) — Migration job ID. Use this to poll status or request cancellation.
`,
		Example: `  flashduty status-page migrate-email-subscribers --data '{"api_key":"sk-stsp-xxxxxxxxxxxxxxxxxxxx","source_page_id":"abcdefghij","target_page_id":5750613685214}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("api-key") {
						body["api_key"] = fAPIKey
					}
					if cmd.Flags().Changed("source-page-id") {
						body["source_page_id"] = fSourcePageID
					}
					if cmd.Flags().Changed("target-page-id") {
						body["target_page_id"] = fTargetPageID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.MigrateStatusPageEmailSubscribersRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.MigrateEmailSubscribers(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAPIKey, "api-key", "", "Atlassian Statuspage API key with access to the source page. (required)")
	cmd.Flags().StringVar(&fSourcePageID, "source-page-id", "", "Atlassian Statuspage source page ID. (required)")
	cmd.Flags().Int64Var(&fTargetPageID, "target-page-id", 0, "Flashduty target status page ID that will receive the imported subscribers. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesMigrateStructureCmd() *cobra.Command {
	var dataJSON string
	var fAPIKey string
	var fSourcePageID string
	var fURLName string
	cmd := &cobra.Command{
		Use:   "migrate-structure",
		Short: "Migrate status page structure",
		Long: `Migrate status page structure.

Start a migration job that imports the structure and historical events of an Atlassian Statuspage into a new Flashduty status page.

API: POST /status-page/migrate-structure (statusPageMigrateStructure)

Request fields:
  --api-key string (required) — Atlassian Statuspage API key with access to the source page.
  --source-page-id string (required) — Atlassian Statuspage source page ID.
  --url-name string — Target URL name for the migrated status page. When omitted, the source page's URL name is reused.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - job_id (string) (required) — Migration job ID. Use this to poll status or request cancellation.
`,
		Example: `  flashduty status-page migrate-structure --data '{"api_key":"sk-stsp-xxxxxxxxxxxxxxxxxxxx","source_page_id":"abcdefghij"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("api-key") {
						body["api_key"] = fAPIKey
					}
					if cmd.Flags().Changed("source-page-id") {
						body["source_page_id"] = fSourcePageID
					}
					if cmd.Flags().Changed("url-name") {
						body["url_name"] = fURLName
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.MigrateStatusPageStructureRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.MigrateStructure(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAPIKey, "api-key", "", "Atlassian Statuspage API key with access to the source page. (required)")
	cmd.Flags().StringVar(&fSourcePageID, "source-page-id", "", "Atlassian Statuspage source page ID. (required)")
	cmd.Flags().StringVar(&fURLName, "url-name", "", "Target URL name for the migrated status page. When omitted, the source page's URL name is reused.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesMigrationCancelCmd() *cobra.Command {
	var dataJSON string
	var fJobID string
	cmd := &cobra.Command{
		Use:   "migration-cancel",
		Short: "Cancel status page migration",
		Long: `Cancel status page migration.

Cancel an in-progress status page migration job. Only jobs currently in the 'running' state can be cancelled.

API: POST /status-page/migration/cancel (statusPageMigrationCancel)

Request fields:
  --job-id string (required) — Migration job ID.
`,
		Example: `  flashduty status-page migration-cancel --data '{"job_id":"01KP0311872NVYFRRQ82FW0001"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("job-id") {
						body["job_id"] = fJobID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.CancelStatusPageMigrationRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.StatusPages.MigrationCancel(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /status-page/migration/cancel")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fJobID, "job-id", "", "Migration job ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesMigrationStatusCmd() *cobra.Command {
	var dataJSON string
	var fJobID string
	cmd := &cobra.Command{
		Use:   "migration-status",
		Short: "Get migration status",
		Long: `Get migration status.

Get the current status and progress of a status page migration job.

API: GET /status-page/migration/status (statusPageMigrationStatus)

Request fields:
  --job-id string (required) — Migration job ID returned by 'migrate-structure' or 'migrate-email-subscribers'.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - account_id (integer) (required) — Owner account ID.
  - created_at (integer) (required) — Job creation time, unix seconds.
  - error (string) — Terminal error message when 'status' is 'failed'.
  - job_id (string) (required) — Migration job ID.
  - phase (string) (required) — Current migration phase. [structure, history, subscribers]
  - progress (object) (required) — Progress counters for a migration job.
    - completed_steps (integer) (required) — Steps completed so far.
    - components_imported (integer) (required)
    - incidents_imported (integer) (required)
    - maintenances_imported (integer) (required)
    - sections_imported (integer) (required)
    - subscribers_imported (integer) (required)
    - subscribers_skipped (integer) (required) — Number of subscribers skipped (e.g. because they would create duplicates).
    - templates_imported (integer) (required)
    - total_steps (integer) (required) — Total steps this job will perform.
    - warnings (array<string>) — Non-fatal warnings recorded during the job.
  - source_page_id (string) (required) — Atlassian Statuspage source page ID.
  - status (string) (required) — Current job status. [pending, running, completed, failed, cancelled]
  - target_page_id (integer) (required) — Flashduty target status page ID. Set once the job produces one, or supplied up front for subscriber migration.
  - updated_at (integer) (required) — Last status update time, unix seconds.
`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("job-id") {
						body["job_id"] = fJobID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.StatusPagesMigrationStatusRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.MigrationStatus(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fJobID, "job-id", "", "Migration job ID returned by 'migrate-structure' or 'migrate-email-subscribers'. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesSubscriberExportCmd() *cobra.Command {
	var dataJSON string
	var fComponentIDs []string
	var fPageID int64
	cmd := &cobra.Command{
		Use:   "subscriber-export",
		Short: "Export subscribers",
		Long: `Export subscribers.

Export subscribers list for a status page as a CSV attachment. The response is a 'text/csv' file with columns: Method, Recipient, Components, Subscribe All, Locale.

API: POST /status-page/subscriber/export (statusPageSubscriberExport)

Request fields:
  --component-ids []string — Optional component IDs to filter subscribers by.
  --page-id int (required) — Status page ID.
`,
		Example: `  flashduty status-page subscriber-export --data '{"page_id":5750613685214}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("component-ids") {
						body["component_ids"] = fComponentIDs
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.ExportStatusPageSubscribersRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.SubscriberExport(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringSliceVar(&fComponentIDs, "component-ids", nil, "Optional component IDs to filter subscribers by.")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesSubscriberImportCmd() *cobra.Command {
	var dataJSON string
	var fMethod string
	var fPageID int64
	cmd := &cobra.Command{
		Use:   "subscriber-import",
		Short: "Import subscribers",
		Long: `Import subscribers.

Bulk import subscribers for a status page.

API: POST /status-page/subscriber/import (statusPageSubscriberImport)

Request fields:
  --method string (required) — Subscription method. 'email' is only valid for public pages; 'im' is only valid for internal pages. [email, im]
  --page-id int (required) — Target status page ID.
  subscribers (array<object>, via --data) — Subscribers to import.
    - all (boolean) — When true, the subscriber receives notifications for all components. Must be true when 'component_ids' and 'change_ids' are both empty.
    - change_ids (array<integer>) — Specific event IDs the subscriber should receive notifications for.
    - component_ids (array<string>) — Component IDs the subscriber should receive notifications for.
    - locale (string) — Preferred locale for notifications. Defaults to the request locale when omitted.
    - recipient (string) (required) — Email address (for public pages) or user ID (for internal pages). (≤255 chars)
`,
		Example: `  flashduty status-page subscriber-import --data '{"method":"email","page_id":5750613685214,"subscribers":[{"all":true,"locale":"en-US","recipient":"alice@example.com"},{"all":false,"component_ids":["01KC3GAZ6ZJE40H55GM31RPWZE"],"locale":"zh-CN","recipient":"bob@example.com"}]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("method") {
						body["method"] = fMethod
					}
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.ImportStatusPageSubscribersRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.StatusPages.SubscriberImport(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /status-page/subscriber/import")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fMethod, "method", "", "Subscription method. 'email' is only valid for public pages; 'im' is only valid for internal pages. (required) [email, im]")
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Target status page ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genStatusPagesSubscriberListCmd() *cobra.Command {
	var dataJSON string
	var fPageID int64
	var fComponentIDs string
	var fP int64
	var fLimit int64
	cmd := &cobra.Command{
		Use:   "subscriber-list",
		Short: "List status page subscribers",
		Long: `List status page subscribers.

List subscribers who have signed up for status page notifications.

API: GET /status-page/subscriber/list (statusPageSubscriberList)

Request fields:
  --page-id int (required) — Status page ID.
  --component-ids string — Comma-separated component IDs to filter subscribers by.
  --page int — Page number (1-based). (min 1)
  --limit int — Page size (1-100). (1-100)

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - has_next_page (boolean) (required) — Whether there is at least one more page after the current one.
  - items (array<object>) (required)
    - all (boolean) (required) — Whether the subscriber is subscribed to all components.
    - components (array<object>) (required) — Components this subscriber has subscribed to.
      - available_since_seconds (integer) — Timestamp when the component was first available, in unix seconds.
      - component_id (string) — Component ID.
      - description (string) — Component description.
      - hide_all (boolean) — When true, the component is hidden entirely from summary endpoints.
      - hide_uptime (boolean) — When true, uptime data is hidden from summary responses.
      - name (string) (required) — Component display name.
      - order_id (integer) — Display order within its section.
      - section_id (string) — Parent section ID.
    - locale (string) — Preferred locale for notifications.
    - method (string) (required) — Subscription delivery method. [email, im]
    - recipient (string) (required) — Subscriber recipient: email address for public pages, user ID for internal pages.
  - total (integer) (required) — Total matching subscribers.
`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page-id") {
						body["page_id"] = fPageID
					}
					if cmd.Flags().Changed("component-ids") {
						body["component_ids"] = fComponentIDs
					}
					if cmd.Flags().Changed("page") {
						body["p"] = fP
					}
					if cmd.Flags().Changed("limit") {
						body["limit"] = fLimit
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.StatusPagesSubscriberListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.StatusPages.SubscriberList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fPageID, "page-id", 0, "Status page ID. (required)")
	cmd.Flags().StringVar(&fComponentIDs, "component-ids", "", "Comma-separated component IDs to filter subscribers by.")
	cmd.Flags().Int64Var(&fP, "page", 0, "Page number (1-based). (min 1)")
	cmd.Flags().Int64Var(&fLimit, "limit", 0, "Page size (1-100). (1-100)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func registerGeneratedStatusPages(root *cobra.Command) {
	gStatusPage := genGroup(root, "status-page", "On-call/Status pages API")
	genAddLeaf(gStatusPage, genStatusPagesReadPageListCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeActiveListCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeCreateCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeDeleteCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeInfoCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeListCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeTimelineCreateCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeTimelineDeleteCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeTimelineUpdateCmd())
	genAddLeaf(gStatusPage, genStatusPagesChangeUpdateCmd())
	genAddLeaf(gStatusPage, genStatusPagesMigrateEmailSubscribersCmd())
	genAddLeaf(gStatusPage, genStatusPagesMigrateStructureCmd())
	genAddLeaf(gStatusPage, genStatusPagesMigrationCancelCmd())
	genAddLeaf(gStatusPage, genStatusPagesMigrationStatusCmd())
	genAddLeaf(gStatusPage, genStatusPagesSubscriberExportCmd())
	genAddLeaf(gStatusPage, genStatusPagesSubscriberImportCmd())
	genAddLeaf(gStatusPage, genStatusPagesSubscriberListCmd())
}
