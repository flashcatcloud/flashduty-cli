// Code generated by internal/cmd/cligen; DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"

	flashduty "github.com/flashcatcloud/go-flashduty"
)

func genAlertsEventReadListCmd() *cobra.Command {
	var dataJSON string
	var fP int64
	var fLimit int64
	var fSearchAfterCtx string
	var fAsc bool
	var fChannelIDs []int
	var fEndTime string
	var fIntegrationIDs []int
	var fIntegrationTypes []string
	var fOrderby string
	var fSeverities string
	var fStartTime string
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List raw alert events",
		Long: `List raw alert events.

Return a cursor-paginated list of raw alert events across all alerts, with filtering by integration, channel, time range, and severity.

API: POST /alert-event/list (alert-event-read-list)

Request fields:
  --page int — Page number, starting at 1. Used when 'search_after_ctx' is not provided.
  --limit int — Page size, max 100, default 20.
  --search-after-ctx string — Opaque cursor for the next page.
  --asc bool — Sort ascending when 'true'.
  --channel-ids []int — Filter by channel IDs. Max 100.
  --end-time string — End of search window, Unix epoch seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --integration-ids []int — Filter by integration IDs.
  --integration-types []string — Filter by integration types (plugin keys).
  --orderby string — Sort field (ES field name). [event_time]
  --severities string — Comma-separated severity filter, e.g. 'Critical,Warning'.
  --start-time string — Start of search window, Unix epoch seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - has_next_page (boolean)
  - items (array<object>)
    - account_id (integer) — Account ID.
    - alert_id (string) — Parent alert ID (MongoDB ObjectID).
    - alert_key (string) — Deduplication key used to merge events into an alert.
    - channel_id (integer) — Channel ID the event is routed to.
    - created_at (integer) — Record creation time, Unix epoch seconds.
    - data_source_id (integer) — Deprecated. Use 'integration_id' instead.
    - deleted_at (integer) — Soft-delete timestamp (seconds). Zero if not deleted.
    - description (string) — Event description.
    - event_id (string) — Event ID (MongoDB ObjectID).
    - event_severity (string) — Severity of this event. [Critical, Warning, Info, Ok]
    - event_status (string) — Status of this event. [Critical, Warning, Info, Ok]
    - event_time (integer) — Event timestamp, Unix epoch seconds.
    - images (array<object>) — Images attached to the event.
      - alt (string) — Alt text.
      - href (string) — Optional link URL when the image is clicked.
      - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
    - integration_id (integer) — Integration that produced this event.
    - integration_type (string) — Type/plugin key of the integration that produced this event.
    - labels (object) — Label key-value pairs.
    - title (string) — Event title.
    - title_rule (string) — Title template used to derive 'title' from labels.
    - updated_at (integer) — Record update time, Unix epoch seconds.
  - search_after_ctx (string)
  - total (integer)
`,
		Example: `  flashduty alert-event list --data '{"end_time":1712707200,"limit":20,"severities":"Critical","start_time":1712620800}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page") {
						body["p"] = fP
					}
					if cmd.Flags().Changed("limit") {
						body["limit"] = fLimit
					}
					if cmd.Flags().Changed("search-after-ctx") {
						body["search_after_ctx"] = fSearchAfterCtx
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("integration-ids") {
						body["integration_ids"] = fIntegrationIDs
					}
					if cmd.Flags().Changed("integration-types") {
						body["integration_types"] = fIntegrationTypes
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertEventGlobalListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.EventReadList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fP, "page", 0, "Page number, starting at 1. Used when 'search_after_ctx' is not provided.")
	cmd.Flags().Int64Var(&fLimit, "limit", 0, "Page size, max 100, default 20.")
	cmd.Flags().StringVar(&fSearchAfterCtx, "search-after-ctx", "", "Opaque cursor for the next page.")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true'.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. Max 100.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End of search window, Unix epoch seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fIntegrationIDs, "integration-ids", nil, "Filter by integration IDs.")
	cmd.Flags().StringSliceVar(&fIntegrationTypes, "integration-types", nil, "Filter by integration types (plugin keys).")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Sort field (ES field name). [event_time]")
	cmd.Flags().StringVar(&fSeverities, "severities", "", "Comma-separated severity filter, e.g. 'Critical,Warning'.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start of search window, Unix epoch seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadEventListCmd() *cobra.Command {
	var dataJSON string
	var fAlertID string
	cmd := &cobra.Command{
		Use:   "event-list",
		Short: "List events for an alert",
		Long: `List events for an alert.

Return all raw events that have been ingested into a specific alert, in chronological order.

API: POST /alert/event/list (alert-read-event-list)

Request fields:
  --alert-id string (required) — Alert ID (ObjectID hex string).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - account_id (integer) — Account ID.
    - alert_id (string) — Parent alert ID (MongoDB ObjectID).
    - alert_key (string) — Deduplication key used to merge events into an alert.
    - channel_id (integer) — Channel ID the event is routed to.
    - created_at (integer) — Record creation time, Unix epoch seconds.
    - data_source_id (integer) — Deprecated. Use 'integration_id' instead.
    - deleted_at (integer) — Soft-delete timestamp (seconds). Zero if not deleted.
    - description (string) — Event description.
    - event_id (string) — Event ID (MongoDB ObjectID).
    - event_severity (string) — Severity of this event. [Critical, Warning, Info, Ok]
    - event_status (string) — Status of this event. [Critical, Warning, Info, Ok]
    - event_time (integer) — Event timestamp, Unix epoch seconds.
    - images (array<object>) — Images attached to the event.
      - alt (string) — Alt text.
      - href (string) — Optional link URL when the image is clicked.
      - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
    - integration_id (integer) — Integration that produced this event.
    - integration_type (string) — Type/plugin key of the integration that produced this event.
    - labels (object) — Label key-value pairs.
    - title (string) — Event title.
    - title_rule (string) — Title template used to derive 'title' from labels.
    - updated_at (integer) — Record update time, Unix epoch seconds.
`,
		Example: `  flashduty alert event-list --data '{"alert_id":"663a1b2c3d4e5f6789abcdef"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("alert-id") {
						body["alert_id"] = fAlertID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertEventListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadEventList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAlertID, "alert-id", "", "Alert ID (ObjectID hex string). (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadFeedCmd() *cobra.Command {
	var dataJSON string
	var fP int64
	var fLimit int64
	var fSearchAfterCtx string
	var fAlertID string
	var fAsc bool
	var fTypes []string
	cmd := &cobra.Command{
		Use:   "feed",
		Short: "List alert activity feed",
		Long: `List alert activity feed.

Return the activity feed (comments, state changes, merges, silence events) for a single alert, with page-based pagination.

API: POST /alert/feed (alert-read-feed)

Request fields:
  --page int — Page number, starting at 1.
  --limit int — Page size, max 100, default 20.
  --search-after-ctx string
  --alert-id string (required) — Alert ID.
  --asc bool — Sort ascending.
  --types []string — Filter by feed types.

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - has_next_page (boolean)
  - items (array<object>)
    - account_id (integer) (required) — Account ID.
    - created_at (integer) (required) — Creation timestamp in Unix epoch milliseconds.
    - creator_id (integer) (required) — Member ID of the creator. 0 for system-generated entries.
    - detail (any) (required) — Type-specific payload. The concrete shape is determined by 'type'.
    - ref_id (string) (required) — ObjectID of the alert this entry references.
    - type (string) (required) — Alert activity feed entry type. Each value identifies one alert lifecycle event; the matching 'detail' payload shape is determined by this field. | Type | Meaning | |---|---| | 'a_new' | Alert triggered. | | 'a_comm' | Comment added on the alert. | | 'a_close' | Alert closed. | [a_new, a_comm, a_close]
    - updated_at (integer) (required) — Last update timestamp in Unix epoch milliseconds.
`,
		Example: `  flashduty alert feed --data '{"alert_id":"663a1b2c3d4e5f6789abcdef","asc":false,"limit":20}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page") {
						body["p"] = fP
					}
					if cmd.Flags().Changed("limit") {
						body["limit"] = fLimit
					}
					if cmd.Flags().Changed("search-after-ctx") {
						body["search_after_ctx"] = fSearchAfterCtx
					}
					if cmd.Flags().Changed("alert-id") {
						body["alert_id"] = fAlertID
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("types") {
						body["types"] = fTypes
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertFeedRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadFeed(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fP, "page", 0, "Page number, starting at 1.")
	cmd.Flags().Int64Var(&fLimit, "limit", 0, "Page size, max 100, default 20.")
	cmd.Flags().StringVar(&fSearchAfterCtx, "search-after-ctx", "", "Request field ")
	cmd.Flags().StringVar(&fAlertID, "alert-id", "", "Alert ID. (required)")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending.")
	cmd.Flags().StringSliceVar(&fTypes, "types", nil, "Filter by feed types.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadInfoCmd() *cobra.Command {
	var dataJSON string
	var fAlertID string
	cmd := &cobra.Command{
		Use:   "info",
		Short: "Get alert detail",
		Long: `Get alert detail.

Return the full details of a single alert by its ID, including its associated incident and event count.

API: POST /alert/info (alert-read-info)

Request fields:
  --alert-id string (required) — Alert ID (ObjectID hex string).

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - account_id (integer) — Account ID.
  - alert_id (string) — Unique alert ID (ObjectID hex string).
  - alert_key (string) — Deduplication key.
  - alert_severity (string) — Current severity. [Critical, Warning, Info, Ok]
  - alert_status (string) — Current status. [Critical, Warning, Info, Ok]
  - channel_id (integer) — ID of the channel the alert belongs to.
  - channel_name (string) — Display name of the channel.
  - channel_status (string) — Status of the channel (e.g. 'enabled', 'disabled').
  - created_at (integer) — Creation timestamp, Unix epoch seconds.
  - data_source_id (integer) — Deprecated. Use 'integration_id' instead. Deprecated: use 'integration_id' instead.
  - data_source_name (string) — Deprecated. Use 'integration_name' instead.
  - data_source_ref_id (string) — Deprecated. Use 'integration_ref_id' instead.
  - data_source_type (string) — Deprecated. Use 'integration_type' instead.
  - description (string) — Alert description.
  - end_time (integer) — Resolution time, Unix epoch seconds. 0 if still active.
  - event_cnt (integer) — Total number of raw events received by this alert.
  - events (array<object>) — Recent raw events attached to this alert. Populated only by some endpoints.
    - account_id (integer) — Account ID.
    - alert_id (string) — Parent alert ID (MongoDB ObjectID).
    - alert_key (string) — Deduplication key used to merge events into an alert.
    - channel_id (integer) — Channel ID the event is routed to.
    - created_at (integer) — Record creation time, Unix epoch seconds.
    - data_source_id (integer) — Deprecated. Use 'integration_id' instead.
    - deleted_at (integer) — Soft-delete timestamp (seconds). Zero if not deleted.
    - description (string) — Event description.
    - event_id (string) — Event ID (MongoDB ObjectID).
    - event_severity (string) — Severity of this event. [Critical, Warning, Info, Ok]
    - event_status (string) — Status of this event. [Critical, Warning, Info, Ok]
    - event_time (integer) — Event timestamp, Unix epoch seconds.
    - images (array<object>) — Images attached to the event.
      - alt (string) — Alt text.
      - href (string) — Optional link URL when the image is clicked.
      - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
    - integration_id (integer) — Integration that produced this event.
    - integration_type (string) — Type/plugin key of the integration that produced this event.
    - labels (object) — Label key-value pairs.
    - title (string) — Event title.
    - title_rule (string) — Title template used to derive 'title' from labels.
    - updated_at (integer) — Record update time, Unix epoch seconds.
  - ever_muted (boolean) — True if this alert has ever been silenced.
  - images (array<object>) — Images attached to the alert.
    - alt (string) — Alt text.
    - href (string) — Optional link URL when the image is clicked.
    - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
  - incident (object) — Brief incident reference embedded in an alert.
    - incident_id (string) — Incident ID (ObjectID hex string).
    - progress (string) — Incident progress — one of 'Triggered', 'Processing', 'Closed'.
    - title (string) — Incident title.
  - integration_id (integer) — ID of the integration that produced this alert.
  - integration_name (string) — Display name of the integration.
  - integration_ref_id (string) — External reference ID of the integration.
  - integration_type (string) — Type/plugin key of the integration.
  - labels (object) — Label key-value pairs.
  - last_time (integer) — Last-event time, Unix epoch seconds.
  - responder_email (string) — Email of the current responder (from the associated incident).
  - responder_name (string) — Display name of the current responder (from the associated incident).
  - start_time (integer) — First-seen time, Unix epoch seconds.
  - title (string) — Alert title.
  - title_rule (string) — Title template used to derive 'title' from the event labels (e.g. '$service::$cluster').
  - updated_at (integer) — Last update timestamp, Unix epoch seconds.
`,
		Example: `  flashduty alert info --data '{"alert_id":"663a1b2c3d4e5f6789abcdef"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("alert-id") {
						body["alert_id"] = fAlertID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertInfoRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadInfo(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAlertID, "alert-id", "", "Alert ID (ObjectID hex string). (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadListCmd() *cobra.Command {
	var dataJSON string
	var fP int64
	var fLimit int64
	var fSearchAfterCtx string
	var fAlertIDs []string
	var fAlertKeys []string
	var fAlertSeverity string
	var fAsc bool
	var fByUpdatedAt bool
	var fChannelIDs []int
	var fEndTime string
	var fEverMuted bool
	var fIntegrationIDs []int
	var fIsActive bool
	var fOrderby string
	var fStartTime string
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List alerts",
		Long: `List alerts.

Return a cursor-paginated list of alerts matching the given filters.

API: POST /alert/list (alert-read-list)

Request fields:
  --page int — Page number, starting at 1. Used when 'search_after_ctx' is not provided.
  --limit int — Page size. Max 100, default 20.
  --search-after-ctx string — Opaque cursor from the previous response for the next page.
  --alert-ids []string — Filter to specific alert IDs (ObjectID hex strings).
  --alert-keys []string — Filter by alert deduplication keys.
  --alert-severity string — Comma-separated severity filter, e.g. 'Critical,Warning'. Allowed values: 'Critical', 'Warning', 'Info', 'Ok'.
  --asc bool — Sort ascending when 'true'. Default descending.
  --by-updated-at bool — When 'true', the time range filter is applied on 'updated_at' rather than 'start_time'.
  --channel-ids []int — Filter by channel IDs.
  --end-time string (required) — End of the search window, Unix epoch seconds. Max span 31 days. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --ever-muted bool — Filter by whether the alert has ever been silenced.
  --integration-ids []int — Filter by integration IDs.
  --is-active bool — Filter by active ('true') or resolved ('false') status.
  --orderby string — Sort field. [created_at, updated_at]
  --start-time string (required) — Start of the search window, Unix epoch seconds. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - has_next_page (boolean) — True if more pages are available.
  - items (array<object>)
    - account_id (integer) — Account ID.
    - alert_id (string) — Unique alert ID (ObjectID hex string).
    - alert_key (string) — Deduplication key.
    - alert_severity (string) — Current severity. [Critical, Warning, Info, Ok]
    - alert_status (string) — Current status. [Critical, Warning, Info, Ok]
    - channel_id (integer) — ID of the channel the alert belongs to.
    - channel_name (string) — Display name of the channel.
    - channel_status (string) — Status of the channel (e.g. 'enabled', 'disabled').
    - created_at (integer) — Creation timestamp, Unix epoch seconds.
    - data_source_id (integer) — Deprecated. Use 'integration_id' instead. Deprecated: use 'integration_id' instead.
    - data_source_name (string) — Deprecated. Use 'integration_name' instead.
    - data_source_ref_id (string) — Deprecated. Use 'integration_ref_id' instead.
    - data_source_type (string) — Deprecated. Use 'integration_type' instead.
    - description (string) — Alert description.
    - end_time (integer) — Resolution time, Unix epoch seconds. 0 if still active.
    - event_cnt (integer) — Total number of raw events received by this alert.
    - events (array<object>) — Recent raw events attached to this alert. Populated only by some endpoints.
      - account_id (integer) — Account ID.
      - alert_id (string) — Parent alert ID (MongoDB ObjectID).
      - alert_key (string) — Deduplication key used to merge events into an alert.
      - channel_id (integer) — Channel ID the event is routed to.
      - created_at (integer) — Record creation time, Unix epoch seconds.
      - data_source_id (integer) — Deprecated. Use 'integration_id' instead.
      - deleted_at (integer) — Soft-delete timestamp (seconds). Zero if not deleted.
      - description (string) — Event description.
      - event_id (string) — Event ID (MongoDB ObjectID).
      - event_severity (string) — Severity of this event. [Critical, Warning, Info, Ok]
      - event_status (string) — Status of this event. [Critical, Warning, Info, Ok]
      - event_time (integer) — Event timestamp, Unix epoch seconds.
      - images (array<object>) — Images attached to the event.
        - alt (string) — Alt text.
        - href (string) — Optional link URL when the image is clicked.
        - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
      - integration_id (integer) — Integration that produced this event.
      - integration_type (string) — Type/plugin key of the integration that produced this event.
      - labels (object) — Label key-value pairs.
      - title (string) — Event title.
      - title_rule (string) — Title template used to derive 'title' from labels.
      - updated_at (integer) — Record update time, Unix epoch seconds.
    - ever_muted (boolean) — True if this alert has ever been silenced.
    - images (array<object>) — Images attached to the alert.
      - alt (string) — Alt text.
      - href (string) — Optional link URL when the image is clicked.
      - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
    - incident (object) — Brief incident reference embedded in an alert.
      - incident_id (string) — Incident ID (ObjectID hex string).
      - progress (string) — Incident progress — one of 'Triggered', 'Processing', 'Closed'.
      - title (string) — Incident title.
    - integration_id (integer) — ID of the integration that produced this alert.
    - integration_name (string) — Display name of the integration.
    - integration_ref_id (string) — External reference ID of the integration.
    - integration_type (string) — Type/plugin key of the integration.
    - labels (object) — Label key-value pairs.
    - last_time (integer) — Last-event time, Unix epoch seconds.
    - responder_email (string) — Email of the current responder (from the associated incident).
    - responder_name (string) — Display name of the current responder (from the associated incident).
    - start_time (integer) — First-seen time, Unix epoch seconds.
    - title (string) — Alert title.
    - title_rule (string) — Title template used to derive 'title' from the event labels (e.g. '$service::$cluster').
    - updated_at (integer) — Last update timestamp, Unix epoch seconds.
  - search_after_ctx (string) — Cursor for the next page.
  - total (integer) — Total matching alerts.
`,
		Example: `  flashduty alert list --data '{"end_time":1712707200,"is_active":true,"limit":20,"start_time":1712620800}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page") {
						body["p"] = fP
					}
					if cmd.Flags().Changed("limit") {
						body["limit"] = fLimit
					}
					if cmd.Flags().Changed("search-after-ctx") {
						body["search_after_ctx"] = fSearchAfterCtx
					}
					if cmd.Flags().Changed("alert-ids") {
						body["alert_ids"] = fAlertIDs
					}
					if cmd.Flags().Changed("alert-keys") {
						body["alert_keys"] = fAlertKeys
					}
					if cmd.Flags().Changed("alert-severity") {
						body["alert_severity"] = fAlertSeverity
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("by-updated-at") {
						body["by_updated_at"] = fByUpdatedAt
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("ever-muted") {
						body["ever_muted"] = fEverMuted
					}
					if cmd.Flags().Changed("integration-ids") {
						body["integration_ids"] = fIntegrationIDs
					}
					if cmd.Flags().Changed("is-active") {
						body["is_active"] = fIsActive
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fP, "page", 0, "Page number, starting at 1. Used when 'search_after_ctx' is not provided.")
	cmd.Flags().Int64Var(&fLimit, "limit", 0, "Page size. Max 100, default 20.")
	cmd.Flags().StringVar(&fSearchAfterCtx, "search-after-ctx", "", "Opaque cursor from the previous response for the next page.")
	cmd.Flags().StringSliceVar(&fAlertIDs, "alert-ids", nil, "Filter to specific alert IDs (ObjectID hex strings).")
	cmd.Flags().StringSliceVar(&fAlertKeys, "alert-keys", nil, "Filter by alert deduplication keys.")
	cmd.Flags().StringVar(&fAlertSeverity, "alert-severity", "", "Comma-separated severity filter, e.g. 'Critical,Warning'. Allowed values: 'Critical', 'Warning', 'Info', 'Ok'.")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true'. Default descending.")
	cmd.Flags().BoolVar(&fByUpdatedAt, "by-updated-at", false, "When 'true', the time range filter is applied on 'updated_at' rather than 'start_time'.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End of the search window, Unix epoch seconds. Max span 31 days. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().BoolVar(&fEverMuted, "ever-muted", false, "Filter by whether the alert has ever been silenced.")
	cmd.Flags().IntSliceVar(&fIntegrationIDs, "integration-ids", nil, "Filter by integration IDs.")
	cmd.Flags().BoolVar(&fIsActive, "is-active", false, "Filter by active ('true') or resolved ('false') status.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Sort field. [created_at, updated_at]")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start of the search window, Unix epoch seconds. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadListByIDsCmd() *cobra.Command {
	var dataJSON string
	var fAlertIDs []string
	cmd := &cobra.Command{
		Use:   "list-by-ids",
		Short: "List alerts by IDs",
		Long: `List alerts by IDs.

Return the details of multiple alerts by their IDs in a single request.

API: POST /alert/list-by-ids (alert-read-list-by-ids)

Request fields:
  --alert-ids []string (required) — List of alert IDs (ObjectID hex strings).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - has_next_page (boolean) — True if more pages are available.
  - items (array<object>)
    - account_id (integer) — Account ID.
    - alert_id (string) — Unique alert ID (ObjectID hex string).
    - alert_key (string) — Deduplication key.
    - alert_severity (string) — Current severity. [Critical, Warning, Info, Ok]
    - alert_status (string) — Current status. [Critical, Warning, Info, Ok]
    - channel_id (integer) — ID of the channel the alert belongs to.
    - channel_name (string) — Display name of the channel.
    - channel_status (string) — Status of the channel (e.g. 'enabled', 'disabled').
    - created_at (integer) — Creation timestamp, Unix epoch seconds.
    - data_source_id (integer) — Deprecated. Use 'integration_id' instead. Deprecated: use 'integration_id' instead.
    - data_source_name (string) — Deprecated. Use 'integration_name' instead.
    - data_source_ref_id (string) — Deprecated. Use 'integration_ref_id' instead.
    - data_source_type (string) — Deprecated. Use 'integration_type' instead.
    - description (string) — Alert description.
    - end_time (integer) — Resolution time, Unix epoch seconds. 0 if still active.
    - event_cnt (integer) — Total number of raw events received by this alert.
    - events (array<object>) — Recent raw events attached to this alert. Populated only by some endpoints.
      - account_id (integer) — Account ID.
      - alert_id (string) — Parent alert ID (MongoDB ObjectID).
      - alert_key (string) — Deduplication key used to merge events into an alert.
      - channel_id (integer) — Channel ID the event is routed to.
      - created_at (integer) — Record creation time, Unix epoch seconds.
      - data_source_id (integer) — Deprecated. Use 'integration_id' instead.
      - deleted_at (integer) — Soft-delete timestamp (seconds). Zero if not deleted.
      - description (string) — Event description.
      - event_id (string) — Event ID (MongoDB ObjectID).
      - event_severity (string) — Severity of this event. [Critical, Warning, Info, Ok]
      - event_status (string) — Status of this event. [Critical, Warning, Info, Ok]
      - event_time (integer) — Event timestamp, Unix epoch seconds.
      - images (array<object>) — Images attached to the event.
        - alt (string) — Alt text.
        - href (string) — Optional link URL when the image is clicked.
        - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
      - integration_id (integer) — Integration that produced this event.
      - integration_type (string) — Type/plugin key of the integration that produced this event.
      - labels (object) — Label key-value pairs.
      - title (string) — Event title.
      - title_rule (string) — Title template used to derive 'title' from labels.
      - updated_at (integer) — Record update time, Unix epoch seconds.
    - ever_muted (boolean) — True if this alert has ever been silenced.
    - images (array<object>) — Images attached to the alert.
      - alt (string) — Alt text.
      - href (string) — Optional link URL when the image is clicked.
      - src (string) (required) — Image source URL or internal image reference (starts with 'img_' or 'http').
    - incident (object) — Brief incident reference embedded in an alert.
      - incident_id (string) — Incident ID (ObjectID hex string).
      - progress (string) — Incident progress — one of 'Triggered', 'Processing', 'Closed'.
      - title (string) — Incident title.
    - integration_id (integer) — ID of the integration that produced this alert.
    - integration_name (string) — Display name of the integration.
    - integration_ref_id (string) — External reference ID of the integration.
    - integration_type (string) — Type/plugin key of the integration.
    - labels (object) — Label key-value pairs.
    - last_time (integer) — Last-event time, Unix epoch seconds.
    - responder_email (string) — Email of the current responder (from the associated incident).
    - responder_name (string) — Display name of the current responder (from the associated incident).
    - start_time (integer) — First-seen time, Unix epoch seconds.
    - title (string) — Alert title.
    - title_rule (string) — Title template used to derive 'title' from the event labels (e.g. '$service::$cluster').
    - updated_at (integer) — Last update timestamp, Unix epoch seconds.
  - search_after_ctx (string) — Cursor for the next page.
  - total (integer) — Total matching alerts.
`,
		Example: `  flashduty alert list-by-ids --data '{"alert_ids":["663a1b2c3d4e5f6789abcdef"]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("alert-ids") {
						body["alert_ids"] = fAlertIDs
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertListByIDsRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadListByIDs(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringSliceVar(&fAlertIDs, "alert-ids", nil, "List of alert IDs (ObjectID hex strings). (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadPipelineInfoCmd() *cobra.Command {
	var dataJSON string
	var fIntegrationID int64
	cmd := &cobra.Command{
		Use:   "pipeline-info",
		Short: "Get alert pipeline",
		Long: `Get alert pipeline.

Return the alert processing pipeline configured for a specific integration.

API: POST /alert/pipeline/info (alert-read-pipeline-info)

Request fields:
  --integration-id int (required) — Integration ID.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - created_at (integer) — Creation timestamp, Unix epoch seconds.
  - creator_id (integer) — Member ID who created the pipeline.
  - integration_id (integer) — Integration ID this pipeline applies to.
  - rules (array<object>) — Ordered list of processing rules.
    - if (array<array>) — OR-of-AND filter tree. Outer array is a list of AND groups; the condition passes if **any** AND group matches. Within each AND group, **all** conditions must match.
    - kind (string) — Rule type. [title_reset, description_reset, severity_reset, alert_drop, alert_inhibit]
    - settings (object) — Kind-specific settings. Shape depends on 'kind': - 'title_reset': '{ "title": "<string>" }' - 'description_reset': '{ "description": "<string>" }' - 'severity_reset': '{ "severity": "Critical"|"Warning"|"Info" }' - 'alert_drop': '{}' (empty object) - 'alert_inhibit': '{ "equals": ["<label_key>", ...], "source_filters": <OrFilterGroup> }'
  - status (string) — Pipeline status. Possible values: 'enabled', 'disabled'.
  - updated_at (integer) — Last update timestamp, Unix epoch seconds.
  - updated_by (integer) — Member ID who last updated the pipeline.
`,
		Example: `  flashduty alert pipeline-info --data '{"integration_id":10001}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("integration-id") {
						body["integration_id"] = fIntegrationID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertPipelineInfoRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadPipelineInfo(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fIntegrationID, "integration-id", 0, "Integration ID. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsReadPipelineListCmd() *cobra.Command {
	var dataJSON string
	var fIntegrationIDs []int
	cmd := &cobra.Command{
		Use:   "pipeline-list",
		Short: "List alert pipelines",
		Long: `List alert pipelines.

Return the alert processing pipelines configured for multiple integrations.

API: POST /alert/pipeline/list (alert-read-pipeline-list)

Request fields:
  --integration-ids []int (required) — Integration IDs.

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - created_at (integer) — Creation timestamp, Unix epoch seconds.
    - creator_id (integer) — Member ID who created the pipeline.
    - integration_id (integer) — Integration ID this pipeline applies to.
    - rules (array<object>) — Ordered list of processing rules.
      - if (array<array>) — OR-of-AND filter tree. Outer array is a list of AND groups; the condition passes if **any** AND group matches. Within each AND group, **all** conditions must match.
      - kind (string) — Rule type. [title_reset, description_reset, severity_reset, alert_drop, alert_inhibit]
      - settings (object) — Kind-specific settings. Shape depends on 'kind': - 'title_reset': '{ "title": "<string>" }' - 'description_reset': '{ "description": "<string>" }' - 'severity_reset': '{ "severity": "Critical"|"Warning"|"Info" }' - 'alert_drop': '{}' (empty object) - 'alert_inhibit': '{ "equals": ["<label_key>", ...], "source_filters": <OrFilterGroup> }'
    - status (string) — Pipeline status. Possible values: 'enabled', 'disabled'.
    - updated_at (integer) — Last update timestamp, Unix epoch seconds.
    - updated_by (integer) — Member ID who last updated the pipeline.
`,
		Example: `  flashduty alert pipeline-list --data '{"integration_ids":[10001,10002]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("integration-ids") {
						body["integration_ids"] = fIntegrationIDs
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertPipelineListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Alerts.ReadPipelineList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().IntSliceVar(&fIntegrationIDs, "integration-ids", nil, "Integration IDs. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsWriteMergeCmd() *cobra.Command {
	var dataJSON string
	var fAlertIDs []string
	var fComment string
	var fIncidentID string
	var fOwnerID int64
	var fTitle string
	cmd := &cobra.Command{
		Use:   "merge",
		Short: "Merge alerts into an incident",
		Long: `Merge alerts into an incident.

Associate one or more alerts with an existing incident. If a source alert previously belonged to a different incident and that incident becomes empty after the merge, it will be automatically closed.

API: POST /alert/merge (alert-write-merge)

Request fields:
  --alert-ids []string (required) — Alert IDs to merge.
  --comment string — Optional comment on the merge action.
  --incident-id string (required) — Target incident ID.
  --owner-id int — Optional new owner for the target incident.
  --title string — Optional new title for the target incident.
`,
		Example: `  flashduty alert merge --data '{"alert_ids":["663a1b2c3d4e5f6789abcdef"],"incident_id":"663a000000000000deadbeef"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("alert-ids") {
						body["alert_ids"] = fAlertIDs
					}
					if cmd.Flags().Changed("comment") {
						body["comment"] = fComment
					}
					if cmd.Flags().Changed("incident-id") {
						body["incident_id"] = fIncidentID
					}
					if cmd.Flags().Changed("owner-id") {
						body["owner_id"] = fOwnerID
					}
					if cmd.Flags().Changed("title") {
						body["title"] = fTitle
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertMergeRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Alerts.WriteMerge(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /alert/merge")
				return nil
			})
		},
	}
	cmd.Flags().StringSliceVar(&fAlertIDs, "alert-ids", nil, "Alert IDs to merge. (required)")
	cmd.Flags().StringVar(&fComment, "comment", "", "Optional comment on the merge action.")
	cmd.Flags().StringVar(&fIncidentID, "incident-id", "", "Target incident ID. (required)")
	cmd.Flags().Int64Var(&fOwnerID, "owner-id", 0, "Optional new owner for the target incident.")
	cmd.Flags().StringVar(&fTitle, "title", "", "Optional new title for the target incident.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAlertsWritePipelineUpsertCmd() *cobra.Command {
	var dataJSON string
	var fIntegrationID int64
	cmd := &cobra.Command{
		Use:   "pipeline-upsert",
		Short: "Create or update alert pipeline",
		Long: `Create or update alert pipeline.

Set the alert processing pipeline for an integration. Replaces the existing configuration entirely.

API: POST /alert/pipeline/upsert (alert-write-pipeline-upsert)

Request fields:
  --integration-id int (required) — Integration ID to configure.
  rules (array<object>, via --data) (required) — Rules to apply. Max 50.
    - if (array<array>) — OR-of-AND filter tree. Outer array is a list of AND groups; the condition passes if **any** AND group matches. Within each AND group, **all** conditions must match.
    - kind (string) — Rule type. [title_reset, description_reset, severity_reset, alert_drop, alert_inhibit]
    - settings (object) — Kind-specific settings. Shape depends on 'kind': - 'title_reset': '{ "title": "<string>" }' - 'description_reset': '{ "description": "<string>" }' - 'severity_reset': '{ "severity": "Critical"|"Warning"|"Info" }' - 'alert_drop': '{}' (empty object) - 'alert_inhibit': '{ "equals": ["<label_key>", ...], "source_filters": <OrFilterGroup> }'
`,
		Example: `  flashduty alert pipeline-upsert --data '{"integration_id":10001,"rules":[{"if":null,"kind":"severity_reset","settings":{"severity":"Warning"}}]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("integration-id") {
						body["integration_id"] = fIntegrationID
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.AlertPipelineUpsertRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Alerts.WritePipelineUpsert(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /alert/pipeline/upsert")
				return nil
			})
		},
	}
	cmd.Flags().Int64Var(&fIntegrationID, "integration-id", 0, "Integration ID to configure. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func registerGeneratedAlerts(root *cobra.Command) {
	gAlertEvent := genGroup(root, "alert-event", "On-call/Alerts API")
	genAddLeaf(gAlertEvent, genAlertsEventReadListCmd())
	gAlert := genGroup(root, "alert", "On-call/Alerts API")
	genAddLeaf(gAlert, genAlertsReadEventListCmd())
	genAddLeaf(gAlert, genAlertsReadFeedCmd())
	genAddLeaf(gAlert, genAlertsReadInfoCmd())
	genAddLeaf(gAlert, genAlertsReadListCmd())
	genAddLeaf(gAlert, genAlertsReadListByIDsCmd())
	genAddLeaf(gAlert, genAlertsReadPipelineInfoCmd())
	genAddLeaf(gAlert, genAlertsReadPipelineListCmd())
	genAddLeaf(gAlert, genAlertsWriteMergeCmd())
	genAddLeaf(gAlert, genAlertsWritePipelineUpsertCmd())
}
