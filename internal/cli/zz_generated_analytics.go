// Code generated by internal/cmd/cligen; DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"

	flashduty "github.com/flashcatcloud/go-flashduty"
)

func genAnalyticsByAccountCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "account",
		Short: "Get account-level insight",
		Long: `Get account-level insight.

Return aggregated incident insight metrics for the entire account.

API: POST /insight/account (insightByAccount)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - account_id (integer)
    - acknowledgement_pct (number)
    - channel_id (integer)
    - channel_name (string)
    - hours (string) — Hour bucket when 'split_hours' is enabled. [work, sleep, off]
    - mean_seconds_to_ack (number)
    - mean_seconds_to_close (number)
    - noise_reduction_pct (number)
    - responder_id (integer)
    - responder_name (string)
    - team_id (integer)
    - team_name (string)
    - total_alert_cnt (integer)
    - total_alert_event_cnt (integer)
    - total_engaged_seconds (integer)
    - total_incident_cnt (integer)
    - total_incidents_acknowledged (integer)
    - total_incidents_auto_closed (integer)
    - total_incidents_closed (integer)
    - total_incidents_escalated (integer)
    - total_incidents_manually_closed (integer)
    - total_incidents_manually_escalated (integer)
    - total_incidents_reassigned (integer)
    - total_incidents_timeout_closed (integer)
    - total_incidents_timeout_escalated (integer)
    - total_interruptions (integer)
    - total_notifications (integer)
    - total_seconds_to_ack (integer)
    - total_seconds_to_close (integer)
    - ts (integer) — Aggregation bucket start time, Unix seconds. Present when 'aggregate_unit' is used.
`,
		Example: `  flashduty insight account --data '{"aggregate_unit":"day","end_time":1712604800,"severities":["Critical","Warning"],"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Analytics.ByAccount(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsByChannelCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "channel",
		Short: "Get channel insight",
		Long: `Get channel insight.

Return insight metrics aggregated by channel.

API: POST /insight/channel (insightByChannel)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - account_id (integer)
    - acknowledgement_pct (number)
    - channel_id (integer)
    - channel_name (string)
    - hours (string) — Hour bucket when 'split_hours' is enabled. [work, sleep, off]
    - mean_seconds_to_ack (number)
    - mean_seconds_to_close (number)
    - noise_reduction_pct (number)
    - responder_id (integer)
    - responder_name (string)
    - team_id (integer)
    - team_name (string)
    - total_alert_cnt (integer)
    - total_alert_event_cnt (integer)
    - total_engaged_seconds (integer)
    - total_incident_cnt (integer)
    - total_incidents_acknowledged (integer)
    - total_incidents_auto_closed (integer)
    - total_incidents_closed (integer)
    - total_incidents_escalated (integer)
    - total_incidents_manually_closed (integer)
    - total_incidents_manually_escalated (integer)
    - total_incidents_reassigned (integer)
    - total_incidents_timeout_closed (integer)
    - total_incidents_timeout_escalated (integer)
    - total_interruptions (integer)
    - total_notifications (integer)
    - total_seconds_to_ack (integer)
    - total_seconds_to_close (integer)
    - ts (integer) — Aggregation bucket start time, Unix seconds. Present when 'aggregate_unit' is used.
`,
		Example: `  flashduty insight channel --data '{"aggregate_unit":"day","channel_ids":[4321322010131],"end_time":1712604800,"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Analytics.ByChannel(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsByResponderCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "responder",
		Short: "Get responder insight",
		Long: `Get responder insight.

Return insight metrics aggregated by responder.

API: POST /insight/responder (insightByResponder)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - account_id (integer)
    - acknowledgement_pct (number)
    - channel_id (integer)
    - channel_name (string)
    - hours (string) — Hour bucket when 'split_hours' is enabled. [work, sleep, off]
    - mean_seconds_to_ack (number)
    - responder_id (integer)
    - responder_name (string)
    - team_id (integer)
    - team_name (string)
    - total_engaged_seconds (integer)
    - total_incident_cnt (integer)
    - total_incidents_acknowledged (integer)
    - total_incidents_escalated (integer)
    - total_incidents_manually_escalated (integer)
    - total_incidents_reassigned (integer)
    - total_incidents_timeout_escalated (integer)
    - total_interruptions (integer)
    - total_notifications (integer)
    - total_seconds_to_ack (integer)
    - ts (integer) — Aggregation bucket start time, Unix seconds. Present when 'aggregate_unit' is used.
`,
		Example: `  flashduty insight responder --data '{"aggregate_unit":"day","end_time":1712604800,"responder_ids":[3790925372131],"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Analytics.ByResponder(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsByTeamCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "team",
		Short: "Get team insight",
		Long: `Get team insight.

Return insight metrics aggregated by team.

API: POST /insight/team (insightByTeam)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - account_id (integer)
    - acknowledgement_pct (number)
    - channel_id (integer)
    - channel_name (string)
    - hours (string) — Hour bucket when 'split_hours' is enabled. [work, sleep, off]
    - mean_seconds_to_ack (number)
    - mean_seconds_to_close (number)
    - noise_reduction_pct (number)
    - responder_id (integer)
    - responder_name (string)
    - team_id (integer)
    - team_name (string)
    - total_alert_cnt (integer)
    - total_alert_event_cnt (integer)
    - total_engaged_seconds (integer)
    - total_incident_cnt (integer)
    - total_incidents_acknowledged (integer)
    - total_incidents_auto_closed (integer)
    - total_incidents_closed (integer)
    - total_incidents_escalated (integer)
    - total_incidents_manually_closed (integer)
    - total_incidents_manually_escalated (integer)
    - total_incidents_reassigned (integer)
    - total_incidents_timeout_closed (integer)
    - total_incidents_timeout_escalated (integer)
    - total_interruptions (integer)
    - total_notifications (integer)
    - total_seconds_to_ack (integer)
    - total_seconds_to_close (integer)
    - ts (integer) — Aggregation bucket start time, Unix seconds. Present when 'aggregate_unit' is used.
`,
		Example: `  flashduty insight team --data '{"aggregate_unit":"day","end_time":1712604800,"start_time":1712000000,"team_ids":[4295771902131]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Analytics.ByTeam(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsChannelExportCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "channel-export",
		Short: "Export channel insight",
		Long: `Export channel insight.

Export channel insight metrics as a CSV file. The response is a CSV stream delivered with 'Content-Disposition: attachment' — it is not a JSON envelope.

API: POST /insight/channel/export (insightChannelExport)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).
`,
		Example: `  flashduty insight channel-export --data '{"channel_ids":[4321322010131],"end_time":1712604800,"severities":["Critical","Warning"],"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Analytics.ChannelExport(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /insight/channel/export")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsIncidentExportCmd() *cobra.Command {
	var dataJSON string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime int64
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fStartTime int64
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "incident-export",
		Short: "Export insight incidents",
		Long: `Export insight incidents.

Export the filtered incident analytics list as a CSV file. The response is a CSV stream delivered with 'Content-Disposition: attachment' — it is not a JSON envelope.

API: POST /insight/incident/export (insightIncidentExport)

Request fields:
  --asc bool
  --channel-ids []int
  --description-html-to-text bool
  --end-time int
  --export-fields []string
  --incident-ids []string
  --is-my-team bool
  --orderby string
  --query string
  --responder-ids []int
  --seconds-to-ack-from int
  --seconds-to-ack-to int
  --seconds-to-close-from int
  --seconds-to-close-to int
  --severities []string
  --start-time int
  --team-ids []int
  --time-zone string
  fields (JSON, via --data)
  labels (JSON, via --data)
`,
		Example: `  flashduty insight incident-export --data '{"description_html_to_text":true,"end_time":1712604800,"export_fields":["incident_id","title","severity","created_at","seconds_to_close"],"severities":["Critical","Warning"],"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if cmd.Flags().Changed("end-time") {
						body["end_time"] = fEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("start-time") {
						body["start_time"] = fStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightFilter)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Analytics.IncidentExport(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /insight/incident/export")
				return nil
			})
		},
	}
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Request field ")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Request field ")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Request field ")
	cmd.Flags().Int64Var(&fEndTime, "end-time", 0, "Request field ")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Request field ")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Request field ")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Request field ")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Request field ")
	cmd.Flags().StringVar(&fQuery, "query", "", "Request field ")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Request field ")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Request field ")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Request field ")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Request field ")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Request field ")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Request field ")
	cmd.Flags().Int64Var(&fStartTime, "start-time", 0, "Request field ")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Request field ")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "Request field ")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsIncidentListCmd() *cobra.Command {
	var dataJSON string
	var fP int64
	var fLimit int64
	var fSearchAfterCtx string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "incident-list",
		Short: "List insight incidents",
		Long: `List insight incidents.

Return a paged list of incidents with per-incident handling metrics used by the analytics dashboard.

API: POST /insight/incident/list (insightIncidentList)

Request fields:
  --page int — Page number, starting at 1. Defaults to 1. (min 1)
  --limit int — Page size, between 1 and 100. Defaults to 20. (1-100)
  --search-after-ctx string — Cursor token returned by a previous page. Pass it back to fetch the next page.
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - has_next_page (boolean)
  - items (array<object>)
    - acknowledgements (integer)
    - assigned_to (object) — Current assignment target for the incident.
      - assigned_at (integer) — Unix timestamp (seconds) when this assignment was made.
      - escalate_rule_id (string) — Escalation rule ID (MongoDB ObjectID) driving the assignment.
      - escalate_rule_name (string) — Display name of the escalation rule.
      - id (string) — Internal assignment record ID.
      - layer_idx (integer) — Current level index within the escalation rule.
      - person_ids (array<integer>) — Member IDs assigned directly to this incident.
      - type (string) — Assignment type. [assign, reassign, escalate, reopen]
    - assignments (integer)
    - channel_id (integer)
    - channel_name (string)
    - closed_by (string) [auto, timeout, manually]
    - created_at (integer)
    - creator_id (integer)
    - creator_name (string)
    - description (string)
    - engaged_seconds (integer)
    - escalations (integer)
    - fields (object)
    - hours (string)
    - incident_id (string)
    - interruptions (integer)
    - labels (object)
    - manual_escalations (integer)
    - notifications (integer)
    - progress (string) — Incident progress state — one of 'Triggered', 'Processing', 'Closed'.
    - reassignments (integer)
    - responders (array<object>)
    - seconds_to_ack (integer)
    - seconds_to_close (integer)
    - severity (string) [Critical, Warning, Info, Ok]
    - team_id (integer)
    - team_name (string)
    - timeout_escalations (integer)
    - title (string)
  - search_after_ctx (string) — Cursor token to fetch the next page. Pass it back in the next request's 'search_after_ctx'.
  - total (integer) — Total matching incidents.
`,
		Example: `  flashduty insight incident-list --data '{"end_time":1712604800,"limit":20,"p":1,"severities":["Critical"],"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page") {
						body["p"] = fP
					}
					if cmd.Flags().Changed("limit") {
						body["limit"] = fLimit
					}
					if cmd.Flags().Changed("search-after-ctx") {
						body["search_after_ctx"] = fSearchAfterCtx
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightIncidentListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Analytics.IncidentList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fP, "page", 0, "Page number, starting at 1. Defaults to 1. (min 1)")
	cmd.Flags().Int64Var(&fLimit, "limit", 0, "Page size, between 1 and 100. Defaults to 20. (1-100)")
	cmd.Flags().StringVar(&fSearchAfterCtx, "search-after-ctx", "", "Cursor token returned by a previous page. Pass it back to fetch the next page.")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsResponderExportCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "responder-export",
		Short: "Export responder insight",
		Long: `Export responder insight.

Export responder insight metrics as a CSV file. The response is a CSV stream delivered with 'Content-Disposition: attachment' — it is not a JSON envelope.

API: POST /insight/responder/export (insightResponderExport)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).
`,
		Example: `  flashduty insight responder-export --data '{"end_time":1712604800,"responder_ids":[3790925372131],"severities":["Critical","Warning"],"start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Analytics.ResponderExport(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /insight/responder/export")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsTeamExportCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "team-export",
		Short: "Export team insight",
		Long: `Export team insight.

Export team insight metrics as a CSV file. The response is a CSV stream delivered with 'Content-Disposition: attachment' — it is not a JSON envelope.

API: POST /insight/team/export (insightTeamExport)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --orderby string — Field to sort the underlying incident set by. [created_at]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).
`,
		Example: `  flashduty insight team-export --data '{"end_time":1712604800,"severities":["Critical","Warning"],"start_time":1712000000,"team_ids":[4295771902131]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightQueryRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Analytics.TeamExport(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /insight/team/export")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort the underlying incident set by. [created_at]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genAnalyticsTopkAlertsByLabelCmd() *cobra.Command {
	var dataJSON string
	var fAggregateUnit string
	var fAsc bool
	var fChannelIDs []int
	var fDescriptionHTMLToText bool
	var fEndTime string
	var fExportFields []string
	var fIncidentIDs []string
	var fIsMyTeam bool
	var fK int64
	var fLabel string
	var fOrderby string
	var fQuery string
	var fResponderIDs []int
	var fSecondsToAckFrom int64
	var fSecondsToAckTo int64
	var fSecondsToCloseFrom int64
	var fSecondsToCloseTo int64
	var fSeverities []string
	var fSplitHours bool
	var fStartTime string
	var fTeamIDs []int
	var fTimeZone string
	cmd := &cobra.Command{
		Use:   "alert-topk-by-label",
		Short: "Get top-K alerts grouped by check or resource",
		Long: `Get top-K alerts grouped by check or resource.

Return the top-K alert groups aggregated either by 'check' or by 'resource' label over the specified time range.

API: POST /insight/alert/topk-by-label (insightTopkAlertsByLabel)

Request fields:
  --aggregate-unit string — Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]
  --asc bool — Sort ascending when 'true', descending otherwise.
  --channel-ids []int — Filter by channel IDs. At most 100 entries.
  --description-html-to-text bool — Strip HTML markup from the description column when exporting.
  --end-time string (required) — End time, Unix seconds. Must be greater than 'start_time'. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --export-fields []string — Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]
  --incident-ids []string — Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.
  --is-my-team bool — Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.
  --k int — Number of top entries to return, between 1 and 100.
  --label string (required) — Dimension to aggregate by. [check, resource]
  --orderby string — Field to sort results by. [total_alert_cnt, total_alert_event_cnt]
  --query string — Full-text query applied to incident title and description.
  --responder-ids []int — Filter by responder person IDs. At most 100 entries.
  --seconds-to-ack-from int — Lower bound (inclusive) on time-to-acknowledge, in seconds.
  --seconds-to-ack-to int — Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.
  --seconds-to-close-from int — Lower bound (inclusive) on time-to-close, in seconds.
  --seconds-to-close-to int — Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.
  --severities []string — Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]
  --split-hours bool — When true, metrics are split into 'work'/'sleep'/'off' hour buckets.
  --start-time string (required) — Start time, Unix seconds. Must be greater than 0. Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.
  --team-ids []int — Filter by team IDs. At most 100 entries.
  --time-zone string — IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.
  fields (object, via --data) — Custom-field filters (exact match).
  labels (object, via --data) — Label filters (exact match).

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>)
    - hours (string) — Hour bucket when 'split_hours' is enabled.
    - label (string) — Aggregation key value (check name or resource identifier).
    - total_alert_cnt (integer)
    - total_alert_event_cnt (integer)
`,
		Example: `  flashduty insight alert-topk-by-label --data '{"end_time":1712604800,"k":10,"label":"check","orderby":"total_alert_cnt","start_time":1712000000}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				vEndTime, okEndTime, err := genParseTimeFlag(cmd, "end-time", fEndTime)
				if err != nil {
					return err
				}
				vStartTime, okStartTime, err := genParseTimeFlag(cmd, "start-time", fStartTime)
				if err != nil {
					return err
				}
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("aggregate-unit") {
						body["aggregate_unit"] = fAggregateUnit
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("channel-ids") {
						body["channel_ids"] = fChannelIDs
					}
					if cmd.Flags().Changed("description-html-to-text") {
						body["description_html_to_text"] = fDescriptionHTMLToText
					}
					if okEndTime {
						body["end_time"] = vEndTime
					}
					if cmd.Flags().Changed("export-fields") {
						body["export_fields"] = fExportFields
					}
					if cmd.Flags().Changed("incident-ids") {
						body["incident_ids"] = fIncidentIDs
					}
					if cmd.Flags().Changed("is-my-team") {
						body["is_my_team"] = fIsMyTeam
					}
					if cmd.Flags().Changed("k") {
						body["k"] = fK
					}
					if cmd.Flags().Changed("label") {
						body["label"] = fLabel
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
					if cmd.Flags().Changed("responder-ids") {
						body["responder_ids"] = fResponderIDs
					}
					if cmd.Flags().Changed("seconds-to-ack-from") {
						body["seconds_to_ack_from"] = fSecondsToAckFrom
					}
					if cmd.Flags().Changed("seconds-to-ack-to") {
						body["seconds_to_ack_to"] = fSecondsToAckTo
					}
					if cmd.Flags().Changed("seconds-to-close-from") {
						body["seconds_to_close_from"] = fSecondsToCloseFrom
					}
					if cmd.Flags().Changed("seconds-to-close-to") {
						body["seconds_to_close_to"] = fSecondsToCloseTo
					}
					if cmd.Flags().Changed("severities") {
						body["severities"] = fSeverities
					}
					if cmd.Flags().Changed("split-hours") {
						body["split_hours"] = fSplitHours
					}
					if okStartTime {
						body["start_time"] = vStartTime
					}
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
					if cmd.Flags().Changed("time-zone") {
						body["time_zone"] = fTimeZone
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.InsightTopkAlertByLabelRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Analytics.TopkAlertsByLabel(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fAggregateUnit, "aggregate-unit", "", "Aggregate metrics into time buckets. When set, the time range must cover at least 24 hours; 'day' additionally caps the range at 31 days. [day, week, month]")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Sort ascending when 'true', descending otherwise.")
	cmd.Flags().IntSliceVar(&fChannelIDs, "channel-ids", nil, "Filter by channel IDs. At most 100 entries.")
	cmd.Flags().BoolVar(&fDescriptionHTMLToText, "description-html-to-text", false, "Strip HTML markup from the description column when exporting.")
	cmd.Flags().StringVar(&fEndTime, "end-time", "", "End time, Unix seconds. Must be greater than 'start_time'. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().StringSliceVar(&fExportFields, "export-fields", nil, "Subset of CSV column keys to include in the export. At most 50 entries. Only used by the export endpoints. [incident_id, title, severity, progress, channel_id, channel_name, team_id, team_name, created_at, seconds_to_ack, seconds_to_close, closed_by, engaged_seconds, hours, notifications, interruptions, acknowledgements, assignments, reassignments, escalations, manual_escalations, timeout_escalations, assigned_to, responders, description, labels, fields, creator_id, creator_name]")
	cmd.Flags().StringSliceVar(&fIncidentIDs, "incident-ids", nil, "Filter by incident IDs (MongoDB ObjectIDs). At most 100 entries.")
	cmd.Flags().BoolVar(&fIsMyTeam, "is-my-team", false, "Restrict results to teams the caller belongs to. When true and the caller has no teams, the result set is empty.")
	cmd.Flags().Int64Var(&fK, "k", 0, "Number of top entries to return, between 1 and 100.")
	cmd.Flags().StringVar(&fLabel, "label", "", "Dimension to aggregate by. (required) [check, resource]")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Field to sort results by. [total_alert_cnt, total_alert_event_cnt]")
	cmd.Flags().StringVar(&fQuery, "query", "", "Full-text query applied to incident title and description.")
	cmd.Flags().IntSliceVar(&fResponderIDs, "responder-ids", nil, "Filter by responder person IDs. At most 100 entries.")
	cmd.Flags().Int64Var(&fSecondsToAckFrom, "seconds-to-ack-from", 0, "Lower bound (inclusive) on time-to-acknowledge, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToAckTo, "seconds-to-ack-to", 0, "Upper bound (exclusive) on time-to-acknowledge, in seconds. Must be greater than 'seconds_to_ack_from' when both are set.")
	cmd.Flags().Int64Var(&fSecondsToCloseFrom, "seconds-to-close-from", 0, "Lower bound (inclusive) on time-to-close, in seconds.")
	cmd.Flags().Int64Var(&fSecondsToCloseTo, "seconds-to-close-to", 0, "Upper bound (exclusive) on time-to-close, in seconds. Must be greater than 'seconds_to_close_from' when both are set.")
	cmd.Flags().StringSliceVar(&fSeverities, "severities", nil, "Filter by severity. At most 3 entries. [Critical, Warning, Info, Ok]")
	cmd.Flags().BoolVar(&fSplitHours, "split-hours", false, "When true, metrics are split into 'work'/'sleep'/'off' hour buckets.")
	cmd.Flags().StringVar(&fStartTime, "start-time", "", "Start time, Unix seconds. Must be greater than 0. (required) Accepts a duration (7d, 24h), '+7d' for the future, 'now', a date, or Unix seconds.")
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "Filter by team IDs. At most 100 entries.")
	cmd.Flags().StringVar(&fTimeZone, "time-zone", "", "IANA time zone name used to interpret the time range (e.g. 'Asia/Shanghai'). Defaults to the account time zone.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func registerGeneratedAnalytics(root *cobra.Command) {
	gInsight := genGroup(root, "insight", "On-call/Analytics API")
	genAddLeaf(gInsight, genAnalyticsByAccountCmd())
	genAddLeaf(gInsight, genAnalyticsByChannelCmd())
	genAddLeaf(gInsight, genAnalyticsByResponderCmd())
	genAddLeaf(gInsight, genAnalyticsByTeamCmd())
	genAddLeaf(gInsight, genAnalyticsChannelExportCmd())
	genAddLeaf(gInsight, genAnalyticsIncidentExportCmd())
	genAddLeaf(gInsight, genAnalyticsIncidentListCmd())
	genAddLeaf(gInsight, genAnalyticsResponderExportCmd())
	genAddLeaf(gInsight, genAnalyticsTeamExportCmd())
	genAddLeaf(gInsight, genAnalyticsTopkAlertsByLabelCmd())
}
