// Code generated by internal/cmd/cligen; DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"

	flashduty "github.com/flashcatcloud/go-flashduty"
)

func genTeamsReadInfoCmd() *cobra.Command {
	var dataJSON string
	var fRefID string
	var fTeamID int64
	var fTeamName string
	cmd := &cobra.Command{
		Use:   "info",
		Short: "Get team detail",
		Long: `Get team detail.

Return a single team by ID, name, or external reference ID.

API: POST /team/info (team-read-info)

Request fields:
  --ref-id string — External reference ID.
  --team-id int — Team ID.
  --team-name string — Team name.

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - account_id (integer) (required) — Owning account ID.
  - created_at (integer) (required) — Unix epoch seconds the team was created.
  - creator_id (integer) (required) — Member ID of the creator.
  - creator_name (string) (required) — Display name of the creator.
  - description (string) (required) — Free-form description.
  - person_ids (array<integer>) (required) — Member IDs of team members.
  - ref_id (string) (required) — External reference ID for third-party HR system integration.
  - status (string) (required) — Team status. [enabled, disabled]
  - team_id (integer) (required) — Unique team ID.
  - team_name (string) (required) — Team display name. 1–39 characters, unique per account.
  - updated_at (integer) (required) — Unix epoch seconds the team was last updated.
  - updated_by (integer) (required) — Member ID of the last editor.
  - updated_by_name (string) (required) — Display name of the last editor.
`,
		Example: `  flashduty team info --data '{"team_id":1001}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("ref-id") {
						body["ref_id"] = fRefID
					}
					if cmd.Flags().Changed("team-id") {
						body["team_id"] = fTeamID
					}
					if cmd.Flags().Changed("team-name") {
						body["team_name"] = fTeamName
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.TeamInfoRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Teams.ReadInfo(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fRefID, "ref-id", "", "External reference ID.")
	cmd.Flags().Int64Var(&fTeamID, "team-id", 0, "Team ID.")
	cmd.Flags().StringVar(&fTeamName, "team-name", "", "Team name.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genTeamsReadInfosCmd() *cobra.Command {
	var dataJSON string
	var fTeamIDs []int
	cmd := &cobra.Command{
		Use:   "infos",
		Short: "Batch get teams",
		Long: `Batch get teams.

Return basic info for multiple teams by their IDs in a single request.

API: POST /team/infos (team-read-infos)

Request fields:
  --team-ids []int (required) — List of team IDs to look up. Max 100.

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>) (required)
    - person_ids (array<integer>)
    - team_id (integer)
    - team_name (string)
`,
		Example: `  flashduty team infos --data '{"team_ids":[1001,1002]}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("team-ids") {
						body["team_ids"] = fTeamIDs
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.TeamInfosRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Teams.ReadInfos(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().IntSliceVar(&fTeamIDs, "team-ids", nil, "List of team IDs to look up. Max 100. (required)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genTeamsReadListCmd() *cobra.Command {
	var dataJSON string
	var fP int64
	var fLimit int64
	var fSearchAfterCtx string
	var fAsc bool
	var fOrderby string
	var fPersonID int64
	var fQuery string
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List teams",
		Long: `List teams.

Return a paginated list of teams in the current account.

API: POST /team/list (team-read-list)

Request fields:
  --page int — Page number. Default: 1. (min 1)
  --limit int — Page size. Max: 100. Default: 20. (1-100)
  --search-after-ctx string
  --asc bool — Ascending sort order.
  --orderby string — Sort field. [created_at, updated_at, team_name]
  --person-id int — Filter by member ID — return only teams this person belongs to.
  --query string — Substring match on team name.

Response fields ('data' envelope is unwrapped — rows are nested under items[]; pipe 'jq '.items[]'', NOT '.data.items[]'):
  - items (array<object>) (required)
    - account_id (integer) (required) — Owning account ID.
    - created_at (integer) (required) — Unix epoch seconds the team was created.
    - creator_id (integer) (required) — Member ID of the creator.
    - creator_name (string) (required) — Display name of the creator.
    - description (string) (required) — Free-form description.
    - person_ids (array<integer>) (required) — Member IDs of team members.
    - ref_id (string) (required) — External reference ID for third-party HR system integration.
    - status (string) (required) — Team status. [enabled, disabled]
    - team_id (integer) (required) — Unique team ID.
    - team_name (string) (required) — Team display name. 1–39 characters, unique per account.
    - updated_at (integer) (required) — Unix epoch seconds the team was last updated.
    - updated_by (integer) (required) — Member ID of the last editor.
    - updated_by_name (string) (required) — Display name of the last editor.
  - limit (integer) (required) — Page size used.
  - p (integer) (required) — Current page number.
  - total (integer) (required) — Total number of teams matching the filter.
`,
		Example: `  flashduty team list --data '{"asc":false,"limit":20,"orderby":"created_at","p":1}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("page") {
						body["p"] = fP
					}
					if cmd.Flags().Changed("limit") {
						body["limit"] = fLimit
					}
					if cmd.Flags().Changed("search-after-ctx") {
						body["search_after_ctx"] = fSearchAfterCtx
					}
					if cmd.Flags().Changed("asc") {
						body["asc"] = fAsc
					}
					if cmd.Flags().Changed("orderby") {
						body["orderby"] = fOrderby
					}
					if cmd.Flags().Changed("person-id") {
						body["person_id"] = fPersonID
					}
					if cmd.Flags().Changed("query") {
						body["query"] = fQuery
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.TeamListRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Teams.ReadList(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().Int64Var(&fP, "page", 0, "Page number. Default: 1. (min 1)")
	cmd.Flags().Int64Var(&fLimit, "limit", 0, "Page size. Max: 100. Default: 20. (1-100)")
	cmd.Flags().StringVar(&fSearchAfterCtx, "search-after-ctx", "", "Request field ")
	cmd.Flags().BoolVar(&fAsc, "asc", false, "Ascending sort order.")
	cmd.Flags().StringVar(&fOrderby, "orderby", "", "Sort field. [created_at, updated_at, team_name]")
	cmd.Flags().Int64Var(&fPersonID, "person-id", 0, "Filter by member ID — return only teams this person belongs to.")
	cmd.Flags().StringVar(&fQuery, "query", "", "Substring match on team name.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genTeamsWriteDeleteCmd() *cobra.Command {
	var dataJSON string
	var fRefID string
	var fTeamID int64
	var fTeamName string
	cmd := &cobra.Command{
		Use:   "delete",
		Short: "Delete a team",
		Long: `Delete a team.

Permanently delete a team by ID, name, or external reference ID.

API: POST /team/delete (team-write-delete)

Request fields:
  --ref-id string — External reference ID.
  --team-id int — Team ID.
  --team-name string — Team name.
`,
		Example: `  flashduty team delete --data '{"team_id":1001}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("ref-id") {
						body["ref_id"] = fRefID
					}
					if cmd.Flags().Changed("team-id") {
						body["team_id"] = fTeamID
					}
					if cmd.Flags().Changed("team-name") {
						body["team_name"] = fTeamName
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.TeamDeleteRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				resp, err := ctx.Client.Teams.WriteDelete(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				if resp != nil && len(resp.Raw) > 0 {
					return ctx.WriteRaw(resp.Raw)
				}
				ctx.WriteResult("OK: POST /team/delete")
				return nil
			})
		},
	}
	cmd.Flags().StringVar(&fRefID, "ref-id", "", "External reference ID.")
	cmd.Flags().Int64Var(&fTeamID, "team-id", 0, "Team ID.")
	cmd.Flags().StringVar(&fTeamName, "team-name", "", "Team name.")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func genTeamsWriteUpsertCmd() *cobra.Command {
	var dataJSON string
	var fCountryCode string
	var fDescription string
	var fEmails []string
	var fPersonIDs []int
	var fPhones []string
	var fRefID string
	var fResetIfNameExist bool
	var fTeamID int64
	var fTeamName string
	cmd := &cobra.Command{
		Use:   "upsert",
		Short: "Create or update a team",
		Long: `Create or update a team.

Create a new team or update an existing one. Pass 'team_id' to update.

API: POST /team/upsert (team-write-upsert)

Request fields:
  --country-code string — Default country code applied to any 'phones' entries that are not in E.164 format.
  --description string — Free-form description. (≤500 chars)
  --emails []string — Email addresses to invite as members.
  --person-ids []int — Member IDs to set as team members. Replaces the existing member list.
  --phones []string — Phone numbers to invite as members.
  --ref-id string — External reference ID for HR system integration.
  --reset-if-name-exist bool — If true and a team with the same name already exists, reset its membership to the provided person_ids.
  --team-id int — Team ID. Omit or set to 0 to create a new team.
  --team-name string (required) — Team display name. 1–39 characters. (1-39 chars)

Response fields ('data' envelope is unwrapped — these fields are at the top level):
  - team_id (integer) (required) — Created or updated team ID.
  - team_name (string) (required) — Team name echoed from the request.
`,
		Example: `  flashduty team upsert --data '{"description":"Backend reliability engineering team","person_ids":[80011,80012],"team_name":"Backend SRE"}'`,
		RunE: func(cmd *cobra.Command, args []string) error {
			return runCommand(cmd, args, func(ctx *RunContext) error {
				body, err := genAssembleBody(dataJSON, func(body map[string]any) {
					if cmd.Flags().Changed("country-code") {
						body["countryCode"] = fCountryCode
					}
					if cmd.Flags().Changed("description") {
						body["description"] = fDescription
					}
					if cmd.Flags().Changed("emails") {
						body["emails"] = fEmails
					}
					if cmd.Flags().Changed("person-ids") {
						body["person_ids"] = fPersonIDs
					}
					if cmd.Flags().Changed("phones") {
						body["phones"] = fPhones
					}
					if cmd.Flags().Changed("ref-id") {
						body["ref_id"] = fRefID
					}
					if cmd.Flags().Changed("reset-if-name-exist") {
						body["reset_if_name_exist"] = fResetIfNameExist
					}
					if cmd.Flags().Changed("team-id") {
						body["team_id"] = fTeamID
					}
					if cmd.Flags().Changed("team-name") {
						body["team_name"] = fTeamName
					}
				})
				if err != nil {
					return err
				}
				req := new(flashduty.TeamUpsertRequest)
				if err := genBindBody(body, req); err != nil {
					return err
				}
				out, _, err := ctx.Client.Teams.WriteUpsert(cmdContext(ctx.Cmd), req)
				if err != nil {
					return err
				}
				return printGenericResult(ctx, out)
			})
		},
	}
	cmd.Flags().StringVar(&fCountryCode, "country-code", "", "Default country code applied to any 'phones' entries that are not in E.164 format.")
	cmd.Flags().StringVar(&fDescription, "description", "", "Free-form description. (≤500 chars)")
	cmd.Flags().StringSliceVar(&fEmails, "emails", nil, "Email addresses to invite as members.")
	cmd.Flags().IntSliceVar(&fPersonIDs, "person-ids", nil, "Member IDs to set as team members. Replaces the existing member list.")
	cmd.Flags().StringSliceVar(&fPhones, "phones", nil, "Phone numbers to invite as members.")
	cmd.Flags().StringVar(&fRefID, "ref-id", "", "External reference ID for HR system integration.")
	cmd.Flags().BoolVar(&fResetIfNameExist, "reset-if-name-exist", false, "If true and a team with the same name already exists, reset its membership to the provided person_ids.")
	cmd.Flags().Int64Var(&fTeamID, "team-id", 0, "Team ID. Omit or set to 0 to create a new team.")
	cmd.Flags().StringVar(&fTeamName, "team-name", "", "Team display name. 1–39 characters. (required) (1-39 chars)")
	cmd.Flags().StringVar(&dataJSON, "data", "", "Full request body as JSON; typed flags override its fields. Accepts inline JSON, or - to read stdin.")
	return cmd
}

func registerGeneratedTeams(root *cobra.Command) {
	gTeam := genGroup(root, "team", "Platform/Teams API")
	genAddLeaf(gTeam, genTeamsReadInfoCmd())
	genAddLeaf(gTeam, genTeamsReadInfosCmd())
	genAddLeaf(gTeam, genTeamsReadListCmd())
	genAddLeaf(gTeam, genTeamsWriteDeleteCmd())
	genAddLeaf(gTeam, genTeamsWriteUpsertCmd())
}
