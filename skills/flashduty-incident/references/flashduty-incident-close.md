# flashduty incident close

Close one or more incidents.

## Usage

```bash
flashduty incident close <id> [<id2> ...]
```

## Flags

No command-specific flags. Supports global flags (`--json`, `--no-trunc`, etc.).

## Examples

```bash
flashduty incident close abc123 def456
```
