# flashduty incident reopen

Reopen one or more closed incidents.

## Usage

```bash
flashduty incident reopen <id> [<id2> ...]
```

## Flags

No command-specific flags. Supports global flags (`--json`, `--no-trunc`, etc.).

## Examples

```bash
flashduty incident reopen abc123
```
